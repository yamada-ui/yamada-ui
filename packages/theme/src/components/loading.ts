import type { ComponentStyle } from "@yamada-ui/core"

export const Loading: ComponentStyle<"Loading"> = {
  baseStyle: {},

  defaultProps: {
    variant: "oval",
    colorScheme: "primary",
    size: "1em",
  },
}
