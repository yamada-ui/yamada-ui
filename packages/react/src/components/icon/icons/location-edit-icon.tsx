import { LocationEdit } from "lucide-react"
import { component, Icon } from "../icon"

/**
 * `LocationEditIcon` is [Lucide](https://lucide.dev) SVG icon component.
 *
 * @see https://yamada-ui.com/components/icon
 */
export const LocationEditIcon = component(Icon)({ as: LocationEdit })
