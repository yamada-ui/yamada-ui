import { a11y, render } from "@yamada-ui/test"
import { {{upperCase packageName}} } from "../src"

describe("<{{upperCase packageName}} />", () => {
  test("{{upperCase packageName}} renders correctly", async () => {
    await a11y(<{{upperCase packageName}} />)
  })
})
