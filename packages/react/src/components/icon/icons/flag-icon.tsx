import { Flag } from "lucide-react"
import { component, Icon } from "../icon"

/**
 * `FlagIcon` is [Lucide](https://lucide.dev) SVG icon component.
 *
 * @see https://yamada-ui.com/components/icon
 */
export const FlagIcon = component(Icon)({ as: Flag })
