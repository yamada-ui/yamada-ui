---
title: useBoolean
description: >-
  `useBoolean` is a custom hook used to manage boolean values using `on`, `off`,
  and `toggle` functions.
storybook: hooks-use-boolean--basic
---

```tsx preview functional
const [flg, { on, off, toggle }] = useBoolean()

return (
  <>
    <Text>state: {String(flg)}</Text>

    <Wrap mt="md" gap="md">
      <Button onClick={on}>On</Button>
      <Button onClick={off}>Off</Button>
      <Button onClick={toggle}>Toggle</Button>
    </Wrap>
  </>
)
```

## Usage

```tsx
import { useBoolean } from "@yamada-ui/react"
```

```tsx
const [flg, { on, off, toggle }] = useBoolean()
```

### Using Initial Values

```tsx preview functional
const [flg, { on, off, toggle }] = useBoolean(true)

return (
  <>
    <Text>state: {String(flg)}</Text>

    <Wrap mt="md" gap="md">
      <Button onClick={on}>On</Button>
      <Button onClick={off}>Off</Button>
      <Button onClick={toggle}>Toggle</Button>
    </Wrap>
  </>
)
```
