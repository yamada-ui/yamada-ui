{
  "name": "@playgrounds/next-app",
  "version": "2.0.0",
  "private": true,
  "scripts": {
    "clean": "rimraf node_modules .next .eslintcache tsconfig.tsbuildinfo",
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "typecheck": "tsc --noEmit",
    "lint": "eslint . --max-warnings=0 --cache",
    "format": "prettier . --ignore-path ../../../.prettierignore --check --cache",
    "yamada-cli": "pnpm -C ../../../packages/cli dev"
  },
  "dependencies": {
    "@yamada-ui/react": "workspace:*",
    "next": "catalog:",
    "react": "catalog:",
    "react-dom": "catalog:"
  },
  "devDependencies": {
    "@yamada-ui/workspace": "workspace:*"
  }
}
