---
"@yamada-ui/cli": minor
---

Added `--dry-run` / `-n` option to all CLI commands (`init`, `add`, `update`, `diff`, `theme`, `tokens`) to preview changes without writing files or installing dependencies.
