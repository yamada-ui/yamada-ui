---
title: Kbd
description: "`Kbd`は、キーボード入力を表すコンポーネントです。"
storybook: components-kbd--basic
source: components/kbd
style: components/kbd/kbd.style.ts
---

```tsx preview
<Text>
  <Kbd>Shift</Kbd> + <Kbd>Tab</Kbd>
</Text>
```

## 使い方

:::code-group

```tsx [package]
import { Kbd } from "@yamada-ui/react"
```

```tsx [alias]
import { Kbd } from "@/components/ui"
```

```tsx [monorepo]
import { Kbd } from "@workspace/ui"
```

:::

```tsx
<Text>
  <Kbd>Shift</Kbd> + <Kbd>Tab</Kbd>
</Text>
```

### バリアントを変更する

```tsx preview
<Wrap gap="md">
  <For each={["solid", "subtle", "surface", "outline"]}>
    {(variant, index) => (
      <Kbd key={index} variant={variant}>
        Shift
      </Kbd>
    )}
  </For>
</Wrap>
```

### サイズを変更する

```tsx preview
<Wrap gap="md">
  <For each={["sm", "md", "lg"]}>
    {(size, index) => (
      <Kbd key={index} size={size}>
        Tab
      </Kbd>
    )}
  </For>
</Wrap>
```

### カラースキームを変更する

```tsx preview
<Wrap gap="md">
  <For each={["green", "pink"]}>
    {(colorScheme, index) => (
      <Kbd key={index} colorScheme={colorScheme}>
        Enter
      </Kbd>
    )}
  </For>
</Wrap>
```
