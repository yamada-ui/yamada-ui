---
title: Table
description: "The `Table` is a component that provides features such as column sorting, row selection, and click events."
package_name: "@yamada-ui/table"
order: 3
with_description: true
tab: Accessibility
---

The `Table` follows the [WAI-ARIA - Table Pattern](https://www.w3.org/WAI/ARIA/apg/patterns/table/) for accessibility.

Setting an `aria-label` on the `Table` allows it to be read by screen readers.

```tsx live=false
<Table columns={columns} data={data} aria-label="DRAGON BALL characters" />
```

## Keyboard Navigation

| Key             | Description                                                                                              | State |
| --------------- | -------------------------------------------------------------------------------------------------------- | ----- |
| `Tab`           | Focuses the first item when focus moves to the table. Focuses the next item if already within the table. | -     |
| `Shift` + `Tab` | Focuses the previous item that is not disabled.                                                          | -     |
| `ArrowLeft`     | Focuses the previous item that is not disabled.                                                          | -     |
| `ArrowRight`    | Focuses the next item that is not disabled.                                                              | -     |
| `ArrowUp`       | Focuses the previous item that is not disabled.                                                          | -     |
| `ArrowDown`     | Focuses the next item that is not disabled.                                                              | -     |
| `Home`          | Focuses the first item that is not disabled.                                                             | -     |
| `End`           | Focuses the last item that is not disabled.                                                              | -     |

## ARIA Roles and Attributes

| Component                | Role and Attributes | Usage                                                                                                                 |
| ------------------------ | ------------------- | --------------------------------------------------------------------------------------------------------------------- |
| `Table`                  | `role="table"`      | Indicates that the element is a table.                                                                                |
|                          | `aria-rowcount`     | Indicates the total number of rows in the table.                                                                      |
|                          | `aria-colcount`     | Indicates the total number of columns in the table.                                                                   |
| `Tr` <Tag>Internal</Tag> | `aria-disabled`     | Set to `"true"` when the row's id is included in `disabledRowIds`.                                                    |
|                          | `aria-selected`     | Set to `"true"` if the row is selected, `"false"` if not selected.                                                    |
| `Th` <Tag>Internal</Tag> | `aria-colindex`     | Indicates which column the element is in the table.                                                                   |
|                          | `aria-rowindex`     | Indicates which row the element is in the table.                                                                      |
|                          | `aria-sort`         | Set to `"ascending"` for ascending order, `"descending"` for descending order, and `"none"` if no order is specified. |
