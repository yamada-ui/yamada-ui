"use client"

import { Book } from "lucide-react"
import { component, Icon } from "../icon"

/**
 * `BookIcon` is [Lucide](https://lucide.dev) SVG icon component.
 *
 * @see https://yamada-ui.com/components/icon
 */
export const BookIcon = component(Icon)({ as: Book })
