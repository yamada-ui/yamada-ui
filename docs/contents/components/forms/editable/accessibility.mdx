---
title: Editable
description: "`Editable` is a component used to obtain inline editable text input."
package_name: "@yamada-ui/editable"
is_tabs: true
order: 2
with_description: true
tab: Accessibility
---

## ARIA Roles and Attributes

| Component          | Role and Attributes | Usage                                     |
| ------------------ | ------------------- | ----------------------------------------- |
| `EditableInput`    | `aria-invalid`      | If Editable is invalid, `“true”` is set.  |
|                    | `aria-disabled`     | If Editable is disabled, `“true”` is set. |
|                    | `aria-readonly`     | If Editable is readonly, `“true”` is set. |
|                    | `aria-required`     | If Editable is required, `“true”` is set. |
| `EditableTextarea` | `aria-invalid`      | If Editable is invalid, `“true”` is set.  |
|                    | `aria-disabled`     | If Editable is disabled, `“true”` is set. |
|                    | `aria-readonly`     | If Editable is readonly, `“true”` is set. |
|                    | `aria-required`     | If Editable is required, `“true”` is set. |
