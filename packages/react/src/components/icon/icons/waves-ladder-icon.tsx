import { WavesLadder } from "lucide-react"
import { component, Icon } from "../icon"

/**
 * `WavesLadderIcon` is [Lucide](https://lucide.dev) SVG icon component.
 *
 * @sees://yamada-ui.com/components/icon
 */
export const WavesLadderIcon = component(Icon)({ as: WavesLadder })
