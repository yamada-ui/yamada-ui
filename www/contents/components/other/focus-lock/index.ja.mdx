---
title: FocusLock
description: >-
  `FocusLock`は、モーダルやダイアログなどの要素でフォーカスを制限し、フォーカスをその範囲内にロックすることで、アクセシビリティを向上させるコンポーネントです。
package_name: "@yamada-ui/focus-lock"
order: 6
is_tabs: true
with_description: true
tab: 使い方
---

## インポート

```ts
import { FocusLock } from "@yamada-ui/react"
```

## 使い方

`FocusLock`は、子要素のフォーカスを制御して、フォーカスをその範囲内にロックします。

```tsx functional=true
const { open, onToggle } = useDisclosure()

return (
  <>
    <Button onClick={onToggle}>Please Click</Button>

    <Collapse open={open}>
      <FocusLock>
        <VStack py="md" px="px">
          <Input placeholder="This is Input" />

          <Button alignSelf="center">Button</Button>
        </VStack>
      </FocusLock>
    </Collapse>
  </>
)
```
