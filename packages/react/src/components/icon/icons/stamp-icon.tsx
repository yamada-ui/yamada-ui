import { Stamp } from "lucide-react"
import { component, Icon } from "../icon"

/**
 * `StampIcon` is [Lucide](https://lucide.dev) SVG icon component.
 *
 * @sees://yamada-ui.com/components/icon
 */
export const StampIcon = component(Icon)({ as: Stamp })
