---
title: Flex
description: "`Flex`は、`Box`に`flex`を設定したコンポーネントです。また、便利なスタイルのショートハンドが用意されています。"
storybook: components-flex--basic
source: components/flex
style: components/flex/flex.style.ts
---

```tsx preview
<Flex gap="md">
  <For each={["info", "success", "warning", "error"]}>
    {(bg, index) => (
      <Box key={index} bg={bg} color="white" p="md" rounded="l2">
        Box
      </Box>
    )}
  </For>
</Flex>
```

## 使い方

:::code-group

```tsx [package]
import { Flex } from "@yamada-ui/react"
```

```tsx [alias]
import { Flex } from "@/components/ui"
```

```tsx [monorepo]
import { Flex } from "@workspace/ui"
```

:::

```tsx
<Flex />
```

### 方向

```tsx preview
<Flex direction="column" gap="md">
  <For each={["info", "success", "warning", "error"]}>
    {(bg, index) => (
      <Box key={index} bg={bg} color="white" p="md" rounded="l2">
        Box
      </Box>
    )}
  </For>
</Flex>
```

### 整列

```tsx preview
<Flex align="center" gap="md">
  <For each={["sm", "md", "lg"]}>
    {(p, index) => (
      <Box key={index} bg={"info"} color="white" p={p} rounded="l2">
        Box
      </Box>
    )}
  </For>
</Flex>
```

### 揃え方

```tsx preview
<Flex justify="space-between" gap="md">
  <For each={["info", "success", "warning", "error"]}>
    {(bg, index) => (
      <Box key={index} bg={bg} color="white" p="md" rounded="l2">
        Box
      </Box>
    )}
  </For>
</Flex>
```

### 折り返し

```tsx preview
<Flex wrap="wrap" gap="md" maxW="xs">
  <For each={["info", "success", "warning", "error"]}>
    {(bg, index) => (
      <Box key={index} bg={bg} color="white" p="md" rounded="l2">
        Box
      </Box>
    )}
  </For>
</Flex>
```

### スペースを追加する

```tsx preview
<Flex w="full" gap="md">
  <Box bg="info" color="white" p="md" rounded="l2">
    Box
  </Box>

  <Spacer />

  <Box bg="error" color="white" p="md" rounded="l2">
    Box
  </Box>
</Flex>
```
