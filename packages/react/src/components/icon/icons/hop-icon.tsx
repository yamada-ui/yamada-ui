import { Hop } from "lucide-react"
import { component, Icon } from "../icon"

/**
 * `HopIcon` is [Lucide](https://lucide.dev) SVG icon component.
 *
 * @sees://yamada-ui.com/components/icon
 */
export const HopIcon = component(Icon)({ as: Hop })
