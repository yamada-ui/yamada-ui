import {
  ui,
  forwardRef,
  useMultiComponentStyle,
  omitThemeProps,
  CSSUIObject,
  HTMLUIProps,
  ThemeProps,
} from '@yamada-ui/core'
import { createContext, cx } from '@yamada-ui/utils'

type {{upperCase packageName}}Context = Record<string, CSSUIObject>

const [{{upperCase packageName}}Provider, use{{upperCase packageName}}] = createContext<{{upperCase packageName}}Context>({
  strict: false,
  name: '{{upperCase packageName}}Context',
})

type {{upperCase packageName}}Options = {}

export type {{upperCase packageName}}Props = HTMLUIProps<'div'> & ThemeProps<'{{upperCase packageName}}'> & {{upperCase packageName}}Options

export const {{upperCase packageName}} = forwardRef<{{upperCase packageName}}Props, 'div'>((props, ref) => {
  const styles = useMultiComponentStyle('{{upperCase packageName}}', props)
  const { className, ...rest } = omitThemeProps(props)

  const css: CSSUIObject = {}

  return (
    <{{upperCase packageName}}Provider value={styles}>
      <ui.div ref={ref} className={cx('ui-{{dashCase packageName}}', className)} __css={css} {...rest} />
    </{{upperCase packageName}}Provider>
  )
})
