import { Mars } from "lucide-react"
import { component, Icon } from "../icon"

/**
 * `MarsIcon` is [Lucide](https://lucide.dev) SVG icon component.
 *
 * @sees://yamada-ui.com/components/icon
 */
export const MarsIcon = component(Icon)({ as: Mars })
