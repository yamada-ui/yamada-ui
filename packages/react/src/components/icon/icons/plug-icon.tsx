import { Plug } from "lucide-react"
import { component, Icon } from "../icon"

/**
 * `PlugIcon` is [Lucide](https://lucide.dev) SVG icon component.
 *
 * @sees://yamada-ui.com/components/icon
 */
export const PlugIcon = component(Icon)({ as: Plug })
