import { Text } from "lucide-react"
import { component, Icon } from "../icon"

/**
 * `TextIcon` is [Lucide](https://lucide.dev) SVG icon component.
 *
 * @sees://yamada-ui.com/components/icon
 */
export const TextIcon = component(Icon)({ as: Text })
