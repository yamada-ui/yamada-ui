import {
  layout,
  color,
  grid,
  flex,
  typography,
  list,
  scroll,
  position,
  background,
  space,
  transform,
  transition,
  filter,
  interactivity,
  effect,
  border,
  others,
} from 'configs'

export const styles = {
  ...layout,
  ...color,
  ...grid,
  ...flex,
  ...typography,
  ...list,
  ...scroll,
  ...position,
  ...background,
  ...space,
  ...transform,
  ...transition,
  ...filter,
  ...interactivity,
  ...effect,
  ...border,
  ...others,
}
