---
title: NumberInput
description: "`NumberInput` is a component used to obtain numeric input from the user."
package_name: "@yamada-ui/number-input"
order: 1
with_description: true
tab: Props
---

## Props

### NumberInputProps

<PropsCard
  id="numberinput-addonprops"
  name="addonProps"
  type="HTMLUIProps"
  description={"Props for addon component."}
/>

<PropsCard
  id="numberinput-allowmousewheel"
  name="allowMouseWheel"
  type="boolean"
  description={"If `true`, the input's value will change based on mouse wheel."}
  defaultValue={"false"}
/>

<PropsCard
  id="numberinput-clampvalueonblur"
  name="clampValueOnBlur"
  type="boolean"
  description={
    "This controls the value update when you blur out of the input.\n- If `true` and the value is greater than `max`, the value will be reset to `max`.\n- Else, the value remains the same."
  }
  defaultValue={"true"}
/>

<PropsCard
  id="numberinput-colorscheme"
  name="colorScheme"
  type='"blackAlpha" | "whiteAlpha" | "amber" | "blue" | "info" | "link" | "primary" | "cyan" | "emerald" | "flashy" | "fuchsia" | "gray" | "green" | "success" | "indigo" | "lime" | "neutral" | "orange" | "warning" | "pink" | "purple" | "red" | "danger" | "rose" | "sky" | "teal" | "violet" | "secondary" | "yellow"'
  description={"The visual color appearance of the component."}
/>

<PropsCard
  id="numberinput-containerprops"
  name="containerProps"
  type="HTMLUIProps"
  description={"Props for container element."}
/>

<PropsCard
  id="numberinput-decrementprops"
  name="decrementProps"
  type="NumberDecrementStepperProps"
  description={"Props for decrement component."}
/>

<PropsCard
  id="numberinput-defaultvalue"
  name="defaultValue"
  type="string | number"
  description={
    "The initial value of the counter.\nShould be less than `max` and greater than `min`."
  }
/>

<PropsCard
  id="numberinput-errorbordercolor"
  name="errorBorderColor"
  type='"border" | "link" | BorderColor | "amber.50" | "amber.100" | "amber.200" | "amber.300" | "amber.400" | "amber.500" | "amber.600" | "amber.700" | "amber.800" | "amber.900" | ... 319 more ... | [...]'
  description={"The border color when the input is invalid."}
/>

<PropsCard
  id="numberinput-focusbordercolor"
  name="focusBorderColor"
  type='"border" | "link" | BorderColor | "amber.50" | "amber.100" | "amber.200" | "amber.300" | "amber.400" | "amber.500" | "amber.600" | "amber.700" | "amber.800" | "amber.900" | ... 319 more ... | [...]'
  description={"The border color when the input is focused."}
/>

<PropsCard
  id="numberinput-focusinputonchange"
  name="focusInputOnChange"
  type="boolean"
  description={
    "If `true`, the input will be focused as you increment or decrement the value with the stepper."
  }
  defaultValue={"true"}
/>

<PropsCard
  id="numberinput-format"
  name="format"
  type="(value: string | number) => string | number"
  description={
    "If using a custom display format, this converts the default format to the custom format."
  }
/>

<PropsCard
  id="numberinput-getariavaluetext"
  name="getAriaValueText"
  type="(value: string | number) => string | undefined"
  description={
    "This is used to format the value so that screen readers\ncan speak out a more human-friendly value.\n\nIt is used to set the `aria-valuetext` property of the input."
  }
/>

<PropsCard
  id="numberinput-incrementprops"
  name="incrementProps"
  type="NumberIncrementStepperProps"
  description={"Props for increment component."}
/>

<PropsCard
  id="numberinput-invalid"
  name="invalid"
  type="boolean"
  description={"If `true`, the form control will be invalid."}
  defaultValue={"false"}
/>

<PropsCard
  id="numberinput-isdisabled"
  name="isDisabled"
  type="boolean"
  description={"If `true`, the form control will be disabled."}
  deprecated={"Use `disabled` instead."}
  defaultValue={"false"}
/>

<PropsCard
  id="numberinput-isinvalid"
  name="isInvalid"
  type="boolean"
  description={"If `true`, the form control will be invalid."}
  deprecated={"Use `invalid` instead."}
  defaultValue={"false"}
/>

<PropsCard
  id="numberinput-isreadonly"
  name="isReadOnly"
  type="boolean"
  description={"If `true`, the form control will be readonly."}
  deprecated={"Use `readOnly` instead."}
  defaultValue={"false"}
/>

<PropsCard
  id="numberinput-isrequired"
  name="isRequired"
  type="boolean"
  description={"If `true`, the form control will be required."}
  deprecated={"Use `required` instead."}
  defaultValue={"false"}
/>

<PropsCard
  id="numberinput-isstepper"
  name="isStepper"
  type="boolean"
  description={"If `true`, display the addon for the number input."}
  deprecated={"Use `stepper` instead."}
/>

<PropsCard
  id="numberinput-isvalidcharacter"
  name="isValidCharacter"
  type="(value: string) => boolean"
  description={
    "Whether the pressed key should be allowed in the input.\nThe default behavior is to allow DOM floating point characters defined by /^[Ee0-9+\-.]$/."
  }
/>

<PropsCard
  id="numberinput-keepwithinrange"
  name="keepWithinRange"
  type="boolean"
  description={
    "This controls the value update behavior in general.\n\n- If `true` and you use the stepper or up/down arrow keys,\nthe value will not exceed the `max` or go lower than `min`.\n\n- If `false`, the value will be allowed to go out of range."
  }
  defaultValue={"true"}
/>

<PropsCard
  id="numberinput-max"
  name="max"
  type="number"
  description={"The maximum value of the counter"}
  defaultValue={"Number.MAX_SAFE_INTEGER"}
/>

<PropsCard
  id="numberinput-min"
  name="min"
  type="number"
  description={"The minimum value of the counter"}
  defaultValue={"Number.MIN_SAFE_INTEGER"}
/>

<PropsCard
  id="numberinput-onchange"
  name="onChange"
  type="(valueAsString: string, valueAsNumber: number) => void"
  description={"The callback fired when the value changes."}
/>

<PropsCard
  id="numberinput-oninvalid"
  name="onInvalid"
  type="(
  message: ValidityState,
  value: string,
  valueAsNumber: number,
) => void"
  description={"The callback invoked when invalid number is entered."}
/>

<PropsCard
  id="numberinput-parse"
  name="parse"
  type="(value: string) => string"
  description={
    "If using a custom display format, this converts the custom format to a format `parseFloat` understands."
  }
/>

<PropsCard
  id="numberinput-precision"
  name="precision"
  type="number"
  description={"The number of decimal points used to round the value."}
/>

<PropsCard
  id="numberinput-size"
  name="size"
  type='"xs" | "sm" | "md" | "lg" | "xl"'
  description={"The size of the NumberInput."}
  defaultValue={'"md"'}
/>

<PropsCard
  id="numberinput-step"
  name="step"
  type="number"
  description={"The step used to increment or decrement the value."}
  defaultValue={"1"}
/>

<PropsCard
  id="numberinput-stepper"
  name="stepper"
  type="boolean"
  description={"If `true`, display the addon for the number input."}
/>

<PropsCard
  id="numberinput-value"
  name="value"
  type="string | number"
  description={
    "The value of the counter.\nShould be less than `max` and greater than `min`."
  }
/>

<PropsCard
  id="numberinput-variant"
  name="variant"
  type='"filled" | "flushed" | "outline" | "unstyled"'
  description={"The variant of the NumberInput."}
  defaultValue={'"outline"'}
/>

### UseNumberInputProps

<PropsCard
  id="usenumberinput-allowmousewheel"
  name="allowMouseWheel"
  type="boolean"
  description={"If `true`, the input's value will change based on mouse wheel."}
  defaultValue={"false"}
/>

<PropsCard
  id="usenumberinput-clampvalueonblur"
  name="clampValueOnBlur"
  type="boolean"
  description={
    "This controls the value update when you blur out of the input.\n- If `true` and the value is greater than `max`, the value will be reset to `max`.\n- Else, the value remains the same."
  }
  defaultValue={"true"}
/>

<PropsCard
  id="usenumberinput-defaultvalue"
  name="defaultValue"
  type="string | number"
  description={
    "The initial value of the counter.\nShould be less than `max` and greater than `min`."
  }
/>

<PropsCard
  id="usenumberinput-disabled"
  name="disabled"
  type="boolean"
  description={"If `true`, the form control will be disabled."}
/>

<PropsCard
  id="usenumberinput-focusinputonchange"
  name="focusInputOnChange"
  type="boolean"
  description={
    "If `true`, the input will be focused as you increment or decrement the value with the stepper."
  }
  defaultValue={"true"}
/>

<PropsCard
  id="usenumberinput-format"
  name="format"
  type="(value: string | number) => string | number"
  description={
    "If using a custom display format, this converts the default format to the custom format."
  }
/>

<PropsCard
  id="usenumberinput-getariavaluetext"
  name="getAriaValueText"
  type="(value: string | number) => string | undefined"
  description={
    "This is used to format the value so that screen readers\ncan speak out a more human-friendly value.\n\nIt is used to set the `aria-valuetext` property of the input."
  }
/>

<PropsCard id="usenumberinput-id" name="id" type="string" />

<PropsCard
  id="usenumberinput-inputmode"
  name="inputMode"
  type='type ONLY_FOR_FORMAT =
  | "text"
  | "search"
  | "decimal"
  | "none"
  | "tel"
  | "url"
  | "email"
  | "numeric"'
  description={
    "Hints at the type of data that might be entered by the user.\nIt also determines the type of keyboard shown to the user on mobile devices."
  }
  defaultValue={"'decimal'"}
/>

<PropsCard
  id="usenumberinput-invalid"
  name="invalid"
  type="boolean"
  description={"If `true`, the form control will be invalid."}
  defaultValue={"false"}
/>

<PropsCard
  id="usenumberinput-isdisabled"
  name="isDisabled"
  type="boolean"
  description={"If `true`, the form control will be disabled."}
  deprecated={"Use `disabled` instead."}
  defaultValue={"false"}
/>

<PropsCard
  id="usenumberinput-isinvalid"
  name="isInvalid"
  type="boolean"
  description={"If `true`, the form control will be invalid."}
  deprecated={"Use `invalid` instead."}
  defaultValue={"false"}
/>

<PropsCard
  id="usenumberinput-isreadonly"
  name="isReadOnly"
  type="boolean"
  description={"If `true`, the form control will be readonly."}
  deprecated={"Use `readOnly` instead."}
  defaultValue={"false"}
/>

<PropsCard
  id="usenumberinput-isrequired"
  name="isRequired"
  type="boolean"
  description={"If `true`, the form control will be required."}
  deprecated={"Use `required` instead."}
  defaultValue={"false"}
/>

<PropsCard
  id="usenumberinput-isvalidcharacter"
  name="isValidCharacter"
  type="(value: string) => boolean"
  description={
    "Whether the pressed key should be allowed in the input.\nThe default behavior is to allow DOM floating point characters defined by /^[Ee0-9+\-.]$/."
  }
/>

<PropsCard
  id="usenumberinput-keepwithinrange"
  name="keepWithinRange"
  type="boolean"
  description={
    "This controls the value update behavior in general.\n\n- If `true` and you use the stepper or up/down arrow keys,\nthe value will not exceed the `max` or go lower than `min`.\n\n- If `false`, the value will be allowed to go out of range."
  }
  defaultValue={"true"}
/>

<PropsCard
  id="usenumberinput-max"
  name="max"
  type="number"
  description={"The maximum value of the counter"}
  defaultValue={"Number.MAX_SAFE_INTEGER"}
/>

<PropsCard
  id="usenumberinput-min"
  name="min"
  type="number"
  description={"The minimum value of the counter"}
  defaultValue={"Number.MIN_SAFE_INTEGER"}
/>

<PropsCard
  id="usenumberinput-name"
  name="name"
  type="string"
  description={"The HTML `name` attribute used for forms."}
/>

<PropsCard
  id="usenumberinput-onblur"
  name="onBlur"
  type="FocusEventHandler<HTMLInputElement>"
/>

<PropsCard
  id="usenumberinput-onchange"
  name="onChange"
  type="(valueAsString: string, valueAsNumber: number) => void"
  description={"The callback fired when the value changes."}
/>

<PropsCard
  id="usenumberinput-onfocus"
  name="onFocus"
  type="FocusEventHandler<HTMLInputElement>"
/>

<PropsCard
  id="usenumberinput-oninvalid"
  name="onInvalid"
  type="(
  message: ValidityState,
  value: string,
  valueAsNumber: number,
) => void"
  description={"The callback invoked when invalid number is entered."}
/>

<PropsCard
  id="usenumberinput-parse"
  name="parse"
  type="(value: string) => string"
  description={
    "If using a custom display format, this converts the custom format to a format `parseFloat` understands."
  }
/>

<PropsCard
  id="usenumberinput-pattern"
  name="pattern"
  type="string"
  description={
    "The pattern used to check the <input> element's value against on form submission."
  }
  defaultValue={"'[0-9]*(.[0-9]+)?'"}
/>

<PropsCard
  id="usenumberinput-precision"
  name="precision"
  type="number"
  description={"The number of decimal points used to round the value."}
/>

<PropsCard
  id="usenumberinput-readonly"
  name="readOnly"
  type="boolean"
  description={"If `true`, the form control will be readonly."}
/>

<PropsCard
  id="usenumberinput-required"
  name="required"
  type="boolean"
  description={"If `true`, the form control will be required."}
/>

<PropsCard
  id="usenumberinput-step"
  name="step"
  type="number"
  description={"The step used to increment or decrement the value."}
  defaultValue={"1"}
/>

<PropsCard
  id="usenumberinput-value"
  name="value"
  type="string | number"
  description={
    "The value of the counter.\nShould be less than `max` and greater than `min`."
  }
/>
