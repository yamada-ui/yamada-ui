import type { CSSUIObject } from "@yamada-ui/react"

export const otherStyle: { [key: string]: CSSUIObject } = {
  title: {
    bgClip: "text",
    bgGradient: "linear(to-r, #59a9e1, #f37bdf)",
    fontSize: "5xl",
  },
}
