"use client"

import { Bot } from "lucide-react"
import { component, Icon } from "../icon"

/**
 * `BotIcon` is [Lucide](https://lucide.dev) SVG icon component.
 *
 * @see https://yamada-ui.com/docs/components/icon
 */
export const BotIcon = component(Icon)({ as: Bot })
