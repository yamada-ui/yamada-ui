---
title: Tag
description: >-
  `Tag` is a component used to categorize or organize items using keywords that
  describe them.
package_name: "@yamada-ui/tag"
order: 3
with_description: true
tab: Accessibility
---

`Tag` displays a close button when `onClose` is set. The `aria-label` of this close button is set to `"Close tag"` by default. If you want to change the `aria-label` to a custom value, set the `aria-label` in `closeButtonProps`.

```tsx live=false
<Tag onClose={() => {}} closeButtonProps={{ "aria-label": "Close custom tag" }}>
  Tag
</Tag>
```

## ARIA Roles and Attributes

| Component                            | Roles and Attributes     | Usage                                           |
| ------------------------------------ | ------------------------ | ----------------------------------------------- |
| `TagCloseButton` <Tag>Internal</Tag> | `aria-label="Close tag"` | If `onClose` is set, then `“Close tag”` is set. |
