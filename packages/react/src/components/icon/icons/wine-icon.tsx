import { Wine } from "lucide-react"
import { component, Icon } from "../icon"

/**
 * `WineIcon` is [Lucide](https://lucide.dev) SVG icon component.
 *
 * @sees://yamada-ui.com/components/icon
 */
export const WineIcon = component(Icon)({ as: Wine })
