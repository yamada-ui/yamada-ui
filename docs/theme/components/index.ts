import { Alert } from "./alert"
import { Button } from "./button"
import { Mark } from "./mark"
import { SegmentedControl } from "./segmented-control"

export const components = { Button, Alert, Mark, SegmentedControl }
