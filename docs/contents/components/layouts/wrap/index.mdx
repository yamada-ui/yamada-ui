---
title: Wrap
description: '`Wrap` is a component that has `wrap` set on `Flex`. It inherits convenient style shorthand from `Flex`.
package_name: '@yamada-ui/layouts'
is_tabs: true
with_description: true
tab: Usage
---

## Import

```ts
import { Wrap } from '@yamada-ui/react'
```

## How to use

```tsx
<Wrap gap='md'>
  {Array(20)
    .fill(0)
    .map((_, index) =>
      ['primary', 'secondary', 'warning', 'danger'].map((bg, nestedIndex) => (
        <Box key={`${index}-${nestedIndex}`} p='md' rounded='md' bg={bg} color='white'>
          Box
        </Box>
      )),
    )}
</Wrap>
```
