import { Menu } from "lucide-react"
import { component, Icon } from "../icon"

/**
 * `MenuIcon` is [Lucide](https://lucide.dev) SVG icon component.
 *
 * @see https://yamada-ui.com/components/icon
 */
export const MenuIcon = component(Icon)({ as: Menu })
