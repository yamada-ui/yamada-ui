---
"@yamada-ui/react": minor
---

`usePopper` now supports the `"center-start-start"`, `"center-start-end"`, `"center-end-start"`, and `"center-end-end"` placements. Accordingly, `Popover` and `Tooltip` now also support these placements.
