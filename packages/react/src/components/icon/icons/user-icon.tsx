import { User } from "lucide-react"
import { component, Icon } from "../icon"

/**
 * `UserIcon` is [Lucide](https://lucide.dev) SVG icon component.
 *
 * @sees://yamada-ui.com/components/icon
 */
export const UserIcon = component(Icon)({ as: User })
