---
title: Image
description: "`Image`は、フォールバックをサポートした画像を表示するコンポーネントです。"
package_name: "@yamada-ui/image"
order: 1
is_tabs: true
with_description: true
tab: 使い方
---

## インポート

```ts
import { Image } from "@yamada-ui/react"
```

## 使い方

```tsx
<Image
  src="https://dragon-ball-official.com/assets/img/intro/intro_1.png"
  boxSize="xl"
/>
```

### フォールバックを使う

画像が読み込み中や読み込みに失敗したときに別の画像を表示したい場合は、`fallback`に表示したい画像のパスを指定します。

```tsx
<Image
  src="https://not-found.com/not-found.png"
  fallback="https://placehold.co/512"
  boxSize="xl"
/>
```

もし、読み込みに失敗したときのみ、`fallback`で指定した画像を表示したい場合は、`fallbackStrategy`に`onError`を指定します。

```tsx
<Image
  src="https://not-found.com/not-found.png"
  fallback="https://placehold.co/512"
  fallbackStrategy="onError"
  boxSize="xl"
/>
```
