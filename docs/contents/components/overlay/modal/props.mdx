---
title: Modal
description: >-
  `Modal` is a component that is displayed over the main content to focus the
  user's attention solely on the information.
package_name: "@yamada-ui/modal"
with_description: true
tab: Props
---

## Props

### Modal Props

<PropsCard name="isOpen"
required
type='boolean'
description='If `true`, the open will be opened.' />
<PropsCard name="allowPinchZoom"
type='boolean'
description='Handle zoom or pinch gestures on iOS devices when scroll locking is enabled.' />
<PropsCard name="animation"
type='"top" | "right" | "bottom" | "left" | "scale" | "none"'
description='The animation of the tooltip.' />
<PropsCard name="blockScrollOnMount"
type='boolean'
description='If `true`, scrolling will be disabled on the `body` when the modal opens.' />
<PropsCard name="closeOnEsc"
type='boolean'
description='If `true`, the modal will close when the `Esc` key is pressed.' />
<PropsCard name="closeOnOverlay"
type='boolean'
description='If `true`, the modal will close when the overlay is clicked.' />
<PropsCard name="colorScheme"
type='"whiteAlpha" | "blackAlpha" | "gray" | "zinc" | "neutral" | "stone" | "red" | "rose" | "pink" | "orange" | "amber" | "yellow" | "lime" | "green" | "emerald" | "teal" | "cyan" | "sky" | "blue" | "indigo" | "violet" | "purple" | "fuchsia"'
description='The visual color appearance of the component.' />
<PropsCard name="duration"
type='number | Partial<Record<"exit" | "enter", number>>'
description='The animation duration.' />
<PropsCard name="finalFocusRef"
type='RefObject<FocusableElement>'
description='`ref` of the element to return focus to when `FocusLock` unmounts.' />
<PropsCard name="initialFocusRef"
type='RefObject<FocusableElement>'
description='`ref` of the element to receive focus initially.' />
<PropsCard name="lockFocusAcrossFrames"
type='boolean'
description='Enables aggressive focus capturing within iframes.
- If `true`: keep focus in the lock, no matter where lock is active.
- If `false`:  allows focus to move outside of iframe.' />
<PropsCard name="onClose"
type='() => void'
description='Callback invoked to close the modal.' />
<PropsCard name="onCloseComplete"
type='() => void'
description='Callback function to run side effects after the modal has closed.' />
<PropsCard name="onEsc"
type='() => void'
description='Callback fired when the escape key is pressed and focus is within modal.' />
<PropsCard name="onOverlayClick"
type='() => void'
description='Callback fired when the overlay is clicked.' />
<PropsCard name="outside"
type='UIValue<number | "px" | (string & {}) | "initial" | "inherit" | "sm" | "md" | "lg" | "xl" | "2xl" | "-moz-initial" | "revert" | "revert-layer" | "unset" | "normal" | "1" | "-1" | "2" | "-2" | ... 72 more ... | "-3.5">'
description='The CSS `padding` property.' />
<PropsCard name="placement"
type='UIValue<| "center"
  | "top"
  | "right"
  | "bottom"
  | "left"
  | "top-left"
  | "top-right"
  | "bottom-left"
  | "bottom-right">'
description='The placement of the modal.' />
<PropsCard name="portalProps"
type='Omit<PortalProps, "children">'
description='Props to be forwarded to the portal component.' />
<PropsCard name="restoreFocus"
type='boolean'
description='If `true`, focus will be restored to the element that triggered the `FocusLock` once it unmounts.' />
<PropsCard name="scrollBehavior"
type='"outside" | "inside"'
description='Where scroll behavior should originate.

- `inside`: scroll only occurs within the `ModalBody`.
- `outside`: the entire `ModalContent` will scroll within the viewport.' />
  <PropsCard
    name="size"
    type='"xs" | "sm" | "md" | "lg" | "xl" | "2xl" | "3xl" | "4xl" | "5xl" | "6xl" | "full"'
    description="The size of the Modal."
  />
  <PropsCard
    name="variant"
    type="string"
    description="The variant of the Modal."
  />
  <PropsCard
    name="withCloseButton"
    type="boolean"
    description="If `true`, display the modal close button."
  />
  <PropsCard
    name="withOverlay"
    type="boolean"
    description="If `true`, display the modal overlay."
  />

### ModalOverlay Props

### ModalCloseButton Props

<PropsCard
  name="isDisabled"
  type="boolean"
  description="If `true`, the button is disabled."
/>

### ModalHeader Props

### ModalBody Props

### ModalFooter Props

### Dialog Props

<PropsCard name="isOpen"
required
type='boolean'
description='If `true`, the open will be opened.' />
<PropsCard name="allowPinchZoom"
type='boolean'
description='Handle zoom or pinch gestures on iOS devices when scroll locking is enabled.' />
<PropsCard name="animation"
type='"top" | "right" | "bottom" | "left" | "scale" | "none"'
description='The animation of the tooltip.' />
<PropsCard name="blockScrollOnMount"
type='boolean'
description='If `true`, scrolling will be disabled on the `body` when the modal opens.' />
<PropsCard name="cancel"
type='type ONLY_FOR_FORMAT =
  | string
  | number
  | boolean
  | ReactElement<any, string | JSXElementConstructor<any>>
  | Iterable<ReactNode>
  | ReactPortal
  | ButtonProps'
description='The dialog cancel to use.' />
<PropsCard name="closeOnEsc"
type='boolean'
description='If `true`, the modal will close when the `Esc` key is pressed.' />
<PropsCard name="closeOnOverlay"
type='boolean'
description='If `true`, the modal will close when the overlay is clicked.' />
<PropsCard name="colorScheme"
type='"whiteAlpha" | "blackAlpha" | "gray" | "zinc" | "neutral" | "stone" | "red" | "rose" | "pink" | "orange" | "amber" | "yellow" | "lime" | "green" | "emerald" | "teal" | "cyan" | "sky" | "blue" | "indigo" | "violet" | "purple" | "fuchsia"'
description='The visual color appearance of the component.' />
<PropsCard name="duration"
type='number | Partial<Record<"exit" | "enter", number>>'
description='The animation duration.' />
<PropsCard name="finalFocusRef"
type='RefObject<FocusableElement>'
description='`ref` of the element to return focus to when `FocusLock` unmounts.' />
<PropsCard name="footer"
type='type ONLY_FOR_FORMAT =
  | string
  | number
  | boolean
  | ReactElement<any, string | JSXElementConstructor<any>>
  | Iterable<ReactNode>
  | ReactPortal'
description='The dialog footer to use.' />
<PropsCard name="header"
type='type ONLY_FOR_FORMAT =
  | string
  | number
  | boolean
  | ReactElement<any, string | JSXElementConstructor<any>>
  | Iterable<ReactNode>
  | ReactPortal'
description='The dialog header to use.' />
<PropsCard name="initialFocusRef"
type='RefObject<FocusableElement>'
description='`ref` of the element to receive focus initially.' />
<PropsCard name="lockFocusAcrossFrames"
type='boolean'
description='Enables aggressive focus capturing within iframes.
- If `true`: keep focus in the lock, no matter where lock is active.
- If `false`:  allows focus to move outside of iframe.' />
<PropsCard name="onCancel"
type='(onClose: (() => void) | undefined) => void'
description='The callback invoked when cancel button clicked.' />
<PropsCard name="onClose"
type='() => void'
description='Callback invoked to close the modal.' />
<PropsCard name="onCloseComplete"
type='() => void'
description='Callback function to run side effects after the modal has closed.' />
<PropsCard name="onEsc"
type='() => void'
description='Callback fired when the escape key is pressed and focus is within modal.' />
<PropsCard name="onOther"
type='(onClose: (() => void) | undefined) => void'
description='The callback invoked when other button clicked.' />
<PropsCard name="onOverlayClick"
type='() => void'
description='Callback fired when the overlay is clicked.' />
<PropsCard name="onSuccess"
type='(onClose: (() => void) | undefined) => void'
description='The callback invoked when success button clicked.' />
<PropsCard name="other"
type='type ONLY_FOR_FORMAT =
  | string
  | number
  | boolean
  | ReactElement<any, string | JSXElementConstructor<any>>
  | Iterable<ReactNode>
  | ReactPortal
  | ButtonProps'
description='The dialog other to use.' />
<PropsCard name="outside"
type='UIValue<number | "px" | (string & {}) | "initial" | "inherit" | "sm" | "md" | "lg" | "xl" | "2xl" | "-moz-initial" | "revert" | "revert-layer" | "unset" | "normal" | "1" | "-1" | "2" | "-2" | ... 72 more ... | "-3.5">'
description='The CSS `padding` property.' />
<PropsCard name="placement"
type='UIValue<| "center"
  | "top"
  | "right"
  | "bottom"
  | "left"
  | "top-left"
  | "top-right"
  | "bottom-left"
  | "bottom-right">'
description='The placement of the modal.' />
<PropsCard name="portalProps"
type='Omit<PortalProps, "children">'
description='Props to be forwarded to the portal component.' />
<PropsCard name="restoreFocus"
type='boolean'
description='If `true`, focus will be restored to the element that triggered the `FocusLock` once it unmounts.' />
<PropsCard name="scrollBehavior"
type='"outside" | "inside"'
description='Where scroll behavior should originate.

- `inside`: scroll only occurs within the `ModalBody`.
- `outside`: the entire `ModalContent` will scroll within the viewport.' />
  <PropsCard
    name="size"
    type='"xs" | "sm" | "md" | "lg" | "xl" | "2xl" | "3xl" | "4xl" | "5xl" | "6xl" | "full"'
    description="The size of the Dialog."
  />
  <PropsCard
    name="success"
    type="type ONLY_FOR_FORMAT =
| string
| number
| boolean
| ReactElement<any, string | JSXElementConstructor<any>>
| Iterable<ReactNode>
| ReactPortal
| ButtonProps"
    description="The dialog success to use."
  />
  <PropsCard
    name="variant"
    type="string"
    description="The variant of the Dialog."
  />
  <PropsCard
    name="withCloseButton"
    type="boolean"
    description="If `true`, display the modal close button."
  />
  <PropsCard
    name="withOverlay"
    type="boolean"
    description="If `true`, display the modal overlay."
  />

### DialogOverlay Props

### DialogCloseButton Props

<PropsCard
  name="isDisabled"
  type="boolean"
  description="If `true`, the button is disabled."
/>

### DialogHeader Props

### DialogBody Props

### DialogFooter Props

### Drawer Props

<PropsCard name="isOpen"
required
type='boolean'
description='If `true`, the open will be opened.' />
<PropsCard name="allowPinchZoom"
type='boolean'
description='Handle zoom or pinch gestures on iOS devices when scroll locking is enabled.' />
<PropsCard name="blockScrollOnMount"
type='boolean'
description='If `true`, scrolling will be disabled on the `body` when the modal opens.' />
<PropsCard name="closeOnEsc"
type='boolean'
description='If `true`, the modal will close when the `Esc` key is pressed.' />
<PropsCard name="closeOnOverlay"
type='boolean'
description='If `true`, the modal will close when the overlay is clicked.' />
<PropsCard name="colorScheme"
type='"whiteAlpha" | "blackAlpha" | "gray" | "zinc" | "neutral" | "stone" | "red" | "rose" | "pink" | "orange" | "amber" | "yellow" | "lime" | "green" | "emerald" | "teal" | "cyan" | "sky" | "blue" | "indigo" | "violet" | "purple" | "fuchsia"'
description='The visual color appearance of the component.' />
<PropsCard name="duration"
type='number | Partial<Record<"exit" | "enter", number>>'
description='The animation duration.' />
<PropsCard name="finalFocusRef"
type='RefObject<FocusableElement>'
description='`ref` of the element to return focus to when `FocusLock` unmounts.' />
<PropsCard name="initialFocusRef"
type='RefObject<FocusableElement>'
description='`ref` of the element to receive focus initially.' />
<PropsCard name="isFullHeight"
type='boolean'
description='If `true` and drawer's placement is `top` or `bottom`, the drawer will occupy the viewport height (100dvh).' />
<PropsCard name="lockFocusAcrossFrames"
type='boolean'
description='Enables aggressive focus capturing within iframes.
- If `true`: keep focus in the lock, no matter where lock is active.
- If `false`:  allows focus to move outside of iframe.' />
<PropsCard name="onClose"
type='() => void'
description='Callback invoked to close the modal.' />
<PropsCard name="onCloseComplete"
type='() => void'
description='Callback function to run side effects after the modal has closed.' />
<PropsCard name="onEsc"
type='() => void'
description='Callback fired when the escape key is pressed and focus is within modal.' />
<PropsCard name="onOverlayClick"
type='() => void'
description='Callback fired when the overlay is clicked.' />
<PropsCard name="placement"
type='UIValue<| "center"
  | "top"
  | "right"
  | "bottom"
  | "left"
  | "top-left"
  | "top-right"
  | "bottom-left"
  | "bottom-right"> &
  UIValue<"top" | "right" | "bottom" | "left">'
description='The placement of the modal.

The placement of the drawer.' />

<PropsCard
  name="portalProps"
  type='Omit<PortalProps, "children">'
  description="Props to be forwarded to the portal component."
/>
<PropsCard
  name="restoreFocus"
  type="boolean"
  description="If `true`, focus will be restored to the element that triggered the `FocusLock` once it unmounts."
/>
<PropsCard
  name="size"
  type='"xs" | "sm" | "md" | "lg" | "xl" | "full"'
  description="The size of the Drawer."
/>
<PropsCard
  name="variant"
  type="string"
  description="The variant of the Drawer."
/>
<PropsCard
  name="withCloseButton"
  type="boolean"
  description="If `true`, display the modal close button."
/>
<PropsCard
  name="withOverlay"
  type="boolean"
  description="If `true`, display the modal overlay."
/>

### DrawerOverlay Props

### DrawerCloseButton Props

<PropsCard
  name="isDisabled"
  type="boolean"
  description="If `true`, the button is disabled."
/>

### DrawerHeader Props

### DrawerBody Props

### DrawerFooter Props
