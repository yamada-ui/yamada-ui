"use client"

import { Tag } from "lucide-react"
import { component, Icon } from "../icon"

/**
 * `TagIcon` is [Lucide](https://lucide.dev) SVG icon component.
 *
 * @see https://yamada-ui.com/components/icon
 */
export const TagIcon = component(Icon)({ as: Tag })
