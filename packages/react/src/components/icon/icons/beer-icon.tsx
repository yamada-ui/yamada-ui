"use client"

import { Beer } from "lucide-react"
import { component, Icon } from "../icon"

/**
 * `BeerIcon` is [Lucide](https://lucide.dev) SVG icon component.
 *
 * @see https://yamada-ui.com/components/icon
 */
export const BeerIcon = component(Icon)({ as: Beer })
