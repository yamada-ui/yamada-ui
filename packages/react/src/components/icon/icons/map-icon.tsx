"use client"

import { Map } from "lucide-react"
import { component, Icon } from "../icon"

/**
 * `MapIcon` is [Lucide](https://lucide.dev) SVG icon component.
 *
 * @see https://yamada-ui.com/components/icon
 */
export const MapIcon = component(Icon)({ as: Map })
