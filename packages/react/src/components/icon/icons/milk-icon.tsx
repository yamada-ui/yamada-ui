import { Milk } from "lucide-react"
import { component, Icon } from "../icon"

/**
 * `MilkIcon` is [Lucide](https://lucide.dev) SVG icon component.
 *
 * @sees://yamada-ui.com/components/icon
 */
export const MilkIcon = component(Icon)({ as: Milk })
