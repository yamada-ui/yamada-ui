---
title: Slider
description: >-
  `Slider` is a component used for allowing users to select a value from a
  range.
package_name: "@yamada-ui/slider"
order: 1
with_description: true
tab: Props
---

## Props

### SliderProps

<PropsCard
  id="slider-colorscheme"
  name="colorScheme"
  type='"blackAlpha" | "whiteAlpha" | "amber" | "blue" | "info" | "link" | "primary" | "cyan" | "emerald" | "flashy" | "fuchsia" | "gray" | "green" | "success" | "indigo" | "lime" | "neutral" | "orange" | "warning" | "pink" | "purple" | "red" | "danger" | "rose" | "sky" | "teal" | "violet" | "secondary" | "yellow"'
  description={"The visual color appearance of the component."}
  defaultValue={'"primary"'}
/>

<PropsCard
  id="slider-defaultvalue"
  name="defaultValue"
  type="number"
  description={"The initial value of the slider."}
/>

<PropsCard
  id="slider-disabled"
  name="disabled"
  type="boolean"
  description={"If `true`, the form control will be disabled."}
  defaultValue={"false"}
/>

<PropsCard
  id="slider-filledtrackcolor"
  name="filledTrackColor"
  type='UIValue<"border" | ({} & string) | Color | "amber.50" | "amber.100" | "amber.200" | "amber.300" | "amber.400" | "amber.500" | "amber.600" | "amber.700" | "amber.800" | "amber.900" | ... 318 more ... | "yellow.950">'
  description={
    "The CSS `color` property. Used in `color` of filled track element."
  }
/>

<PropsCard
  id="slider-filledtrackprops"
  name="filledTrackProps"
  type="SliderFilledTrackProps"
  description={"Props for slider filled track element."}
/>

<PropsCard
  id="slider-focusthumbonchange"
  name="focusThumbOnChange"
  type="boolean"
  description={
    "If `false`, the slider handle will not capture focus when value changes."
  }
  defaultValue={"true"}
/>

<PropsCard
  id="slider-getariavaluetext"
  name="getAriaValueText"
  type="(value: number) => string | undefined"
  description={
    "This is used to format the value so that screen readers\ncan speak out a more human-friendly value.\n\nIt is used to set the `aria-valuetext` property of the input."
  }
/>

<PropsCard
  id="slider-id"
  name="id"
  type="string"
  description={"The base `id` to use for the slider."}
/>

<PropsCard
  id="slider-inputprops"
  name="inputProps"
  type='HTMLUIProps<"input">'
  description={"Props for slider input element."}
/>

<PropsCard
  id="slider-invalid"
  name="invalid"
  type="boolean"
  description={"If `true`, the form control will be invalid."}
  defaultValue={"false"}
/>

<PropsCard
  id="slider-isdisabled"
  name="isDisabled"
  type="boolean"
  description={"If `true`, the form control will be disabled."}
  deprecated={"Use `disabled` instead."}
  defaultValue={"false"}
/>

<PropsCard
  id="slider-isinvalid"
  name="isInvalid"
  type="boolean"
  description={"If `true`, the form control will be invalid."}
  deprecated={"Use `invalid` instead."}
  defaultValue={"false"}
/>

<PropsCard
  id="slider-isreadonly"
  name="isReadOnly"
  type="boolean"
  description={"If `true`, the form control will be readonly."}
  deprecated={"Use `readOnly` instead."}
  defaultValue={"false"}
/>

<PropsCard
  id="slider-isrequired"
  name="isRequired"
  type="boolean"
  description={"If `true`, the form control will be required."}
  deprecated={"Use `required` instead."}
  defaultValue={"false"}
/>

<PropsCard
  id="slider-isreversed"
  name="isReversed"
  type="boolean"
  description={
    "If `true`, the value will be incremented or decremented in reverse."
  }
  deprecated={"Use `reversed` instead."}
/>

<PropsCard
  id="slider-max"
  name="max"
  type="number"
  description={
    "The maximum allowed value of the slider. Cannot be less than min."
  }
  defaultValue={"100"}
/>

<PropsCard
  id="slider-min"
  name="min"
  type="number"
  description={
    "The minimum allowed value of the slider. Cannot be greater than max."
  }
  defaultValue={"0"}
/>

<PropsCard
  id="slider-name"
  name="name"
  type="string"
  description={
    "The name attribute of the hidden `input` field.\nThis is particularly useful in forms."
  }
/>

<PropsCard
  id="slider-onchange"
  name="onChange"
  type="(value: number) => void"
  description={"Function called whenever the slider value changes."}
/>

<PropsCard
  id="slider-onchangeend"
  name="onChangeEnd"
  type="(value: number) => void"
  description={"Function called when the user is done selecting a new value."}
/>

<PropsCard
  id="slider-onchangestart"
  name="onChangeStart"
  type="(value: number) => void"
  description={"Function called when the user starts selecting a new value."}
/>

<PropsCard
  id="slider-orientation"
  name="orientation"
  type='"horizontal" | "vertical"'
  description={"The orientation of the slider."}
  defaultValue={"'horizontal'"}
/>

<PropsCard
  id="slider-readonly"
  name="readOnly"
  type="boolean"
  description={"If `true`, the form control will be readonly."}
  defaultValue={"false"}
/>

<PropsCard
  id="slider-required"
  name="required"
  type="boolean"
  description={"If `true`, the form control will be required."}
  defaultValue={"false"}
/>

<PropsCard
  id="slider-reversed"
  name="reversed"
  type="boolean"
  description={
    "If `true`, the value will be incremented or decremented in reverse."
  }
/>

<PropsCard
  id="slider-size"
  name="size"
  type='"sm" | "md" | "lg"'
  description={"The size of the Slider."}
  defaultValue={'"md"'}
/>

<PropsCard
  id="slider-step"
  name="step"
  type="number"
  description={"The step in which increments or decrements have to be made."}
  defaultValue={"1"}
/>

<PropsCard
  id="slider-thumbcolor"
  name="thumbColor"
  type='UIValue<"border" | ({} & string) | "amber.50" | "amber.100" | "amber.200" | "amber.300" | "amber.400" | "amber.500" | "amber.600" | "amber.700" | "amber.800" | "amber.900" | "amber.950" | ... 318 more ... | Background<...>>'
  description={
    "The CSS `background` property. Used in `background` of thumb element."
  }
/>

<PropsCard
  id="slider-thumbprops"
  name="thumbProps"
  type="SliderThumbProps"
  description={"Props for slider thumb element."}
/>

<PropsCard
  id="slider-thumbsize"
  name="thumbSize"
  type='UIValue<number | "px" | "max" | "min" | "-moz-initial" | "inherit" | "initial" | "revert" | "revert-layer" | "unset" | "-moz-max-content" | "-moz-min-content" | "-webkit-fit-content" | ... 75 more ... | "xs">'
  description={
    "The CSS `box-size` property.\nUsed for calculating the width, height, and percentage of the container element."
  }
/>

<PropsCard
  id="slider-trackcolor"
  name="trackColor"
  type='UIValue<"border" | ({} & string) | Color | "amber.50" | "amber.100" | "amber.200" | "amber.300" | "amber.400" | "amber.500" | "amber.600" | "amber.700" | "amber.800" | "amber.900" | ... 318 more ... | "yellow.950">'
  description={"The CSS `color` property. Used in `color` of track element."}
/>

<PropsCard
  id="slider-trackprops"
  name="trackProps"
  type="SliderTrackProps"
  description={"Props for slider track element."}
/>

<PropsCard
  id="slider-tracksize"
  name="trackSize"
  type='UIValue<number | "px" | "max" | "min" | "-moz-initial" | "inherit" | "initial" | "revert" | "revert-layer" | "unset" | "-moz-max-content" | "-moz-min-content" | "-webkit-fit-content" | ... 71 more ... | "xs">'
  description={"The CSS `height` property. Used in `height` of track element."}
/>

<PropsCard
  id="slider-value"
  name="value"
  type="number"
  description={"The value of the slider."}
/>

<PropsCard
  id="slider-variant"
  name="variant"
  type="string"
  description={"The variant of the Slider."}
/>
