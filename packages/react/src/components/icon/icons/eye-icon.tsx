"use client"

import { Eye } from "lucide-react"
import { component, Icon } from "../icon"

/**
 * `EyeIcon` is [Lucide](https://lucide.dev) SVG icon component.
 *
 * @see https://yamada-ui.com/components/icon
 */
export const EyeIcon = component(Icon)({ as: Eye })
