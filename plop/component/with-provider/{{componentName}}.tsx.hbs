import {
  ui,
  forwardRef,
  useMultiComponentStyle,
  omitThemeProps,
  CSSUIObject,
  HTMLUIProps,
  ThemeProps,
} from '@yamada-ui/core'
import { createContext, cx } from '@yamada-ui/utils'

type {{upperCase componentName}}Context = Record<string, CSSUIObject>

const [{{upperCase componentName}}Provider, use{{upperCase componentName}}] = createContext<{{upperCase componentName}}Context>({
  strict: false,
  name: '{{upperCase componentName}}Context',
})

export { use{{upperCase componentName}} }

type {{upperCase componentName}}Options = {}

export type {{upperCase componentName}}Props = HTMLUIProps<'div'> & ThemeProps<'{{upperCase componentName}}'> & {{upperCase componentName}}Options

export const {{upperCase componentName}} = forwardRef<{{upperCase componentName}}Props, 'div'>((props, ref) => {
  const styles = useMultiComponentStyle('{{upperCase componentName}}', props)
  const { className, ...rest } = omitThemeProps(props)

  const css: CSSUIObject = {}

  return (
    <{{upperCase componentName}}Provider value={styles}>
      <ui.div ref={ref} className={cx('ui-{{dashCase componentName}}', className)} __css={css} {...rest} />
    </{{upperCase componentName}}Provider>
  )
})
