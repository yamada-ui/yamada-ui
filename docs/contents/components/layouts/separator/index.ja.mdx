---
title: Separator
description: "`Separator`は、要素間において区切りを表すコンポーネントです。"
package_name: "@yamada-ui/layouts"
order: 12
is_tabs: true
with_description: true
tab: 使い方
---

## インポート

```ts
import { Separator } from "@yamada-ui/react"
```

## 使い方

```tsx
<Flex direction="column" gap="md">
  <For each={["solid", "dashed", "dotted"]}>
    {(variant, index) => <Separator key={index} variant={variant} />}
  </For>
</Flex>
```

### 垂直に区切り線を引く

```tsx
<Flex h="sm" gap="md">
  <For each={["solid", "dashed", "dotted"]}>
    {(variant, index) => (
      <Separator key={index} orientation="vertical" variant={variant} />
    )}
  </For>
</Flex>
```

:::note status=warning
区切り線を垂直にする場合は、親要素または`Separator`に高さを設定する必要があります。
:::
