---
title: AlphaSlider
description: "`AlphaSlider`は、ユーザーが色相を選択するために使用されるコンポーネントです。"
package_name: "@yamada-ui/color-picker"
order: 3
with_description: true
tab: アクセシビリティ
---

`AlphaSlider`は、アクセシビリティに関して[WAI-ARIA - Slider Pattern](https://www.w3.org/WAI/ARIA/apg/patterns/slider/)に従います。

## キーボード操作

| キー         | 説明                                       | 状態 |
| ------------ | ------------------------------------------ | ---- |
| `ArrowRight` | `step`の値に基づいて値を増加します。       | -    |
| `ArrowLeft`  | `step`の値に基づいて値を減少します。       | -    |
| `ArrowUp`    | `step`の値に基づいて値を増加します。       | -    |
| `ArrowDown`  | `step`の値に基づいて値を減少します。       | -    |
| `Home`       | `min`の値を設定します。                    | -    |
| `End`        | `max`の値を設定します。                    | -    |
| `PageUp`     | `min`と`max`の値に基づいて値を増加します。 | -    |
| `PageDown`   | `min`と`max`の値に基づいて値を減少します。 | -    |

## ARIAロールと属性

| コンポーネント                     | ロールと属性     | 使い方                                                   |
| ---------------------------------- | ---------------- | -------------------------------------------------------- |
| `AlphaSliderThumb` <Tag>内部</Tag> | `role="slider"`  | スライダーであることを示します。                         |
|                                    | `aria-label`     | `"Slider thumb"`を設定します。                           |
|                                    | `aria-valuemin`  | `min`の値を設定します。デフォルトは`0`です。             |
|                                    | `aria-valuemax`  | `max`の値を設定します。デフォルトは`1`です。             |
|                                    | `aria-valuenow`  | 現在の値を設定します。                                   |
|                                    | `aria-valuetext` | `"18%"`のように、現在の値を設定します。                  |
|                                    | `aria-readonly`  | `isReadOnly`が設定されている場合は`"true"`を設定します。 |
|                                    | `aria-disabled`  | `isDisabled`が設定されている場合は`"true"`を設定します。 |
|                                    | `aria-invalid`   | `isInvalid`が設定されている場合は`"true"`を設定します。  |
|                                    | `aria-required`  | `isRequired`が設定されている場合は`"true"`を設定します。 |
| `input` <Tag>内部</Tag>            | `type="hidden"`  | 要素をアクセシビリティツリーから除外します。             |
|                                    | `aria-readonly`  | `isReadOnly`が設定されている場合は`"true"`を設定します。 |
|                                    | `aria-disabled`  | `isDisabled`が設定されている場合は`"true"`を設定します。 |
|                                    | `aria-invalid`   | `isInvalid`が設定されている場合は`"true"`を設定します。  |
|                                    | `aria-required`  | `isRequired`が設定されている場合は`"true"`を設定します。 |
