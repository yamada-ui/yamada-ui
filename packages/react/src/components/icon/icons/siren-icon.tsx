import { Siren } from "lucide-react"
import { component, Icon } from "../icon"

/**
 * `SirenIcon` is [Lucide](https://lucide.dev) SVG icon component.
 *
 * @sees://yamada-ui.com/components/icon
 */
export const SirenIcon = component(Icon)({ as: Siren })
