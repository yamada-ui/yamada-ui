---
"@yamada-ui/lint": minor
---

Initial release of `@yamada-ui/lint`. Adds the `props-shorthand` rule that enforces consistent shorthand or longhand style props on Yamada UI components with auto-fix. Configurable per project via the `preferred` option (`"shorthand"` or `"longhand"`, default: `"shorthand"`).
