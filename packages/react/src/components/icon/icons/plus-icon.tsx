import { Plus } from "lucide-react"
import { component, Icon } from "../icon"

/**
 * `PlusIcon` is [Lucide](https://lucide.dev) SVG icon component.
 *
 * @sees://yamada-ui.com/components/icon
 */
export const PlusIcon = component(Icon)({ as: Plus })
