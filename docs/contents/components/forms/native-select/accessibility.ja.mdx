---
title: NativeSelect
description: >-
  `NativeSelect`は、ユーザーがオプションのリストから1つを選択するために使用されるコンポーネントです。ブラウザ（ユーザーエージェント）によって提供されるネイティブなドロップダウンリストを表示します。
package_name: "@yamada-ui/native-select"
order: 4
with_description: true
tab: アクセシビリティ
---

`NativeSelect` は、アクセシビリティに関して、[WAI-ARIA - Combobox Pattern]("https://www.w3.org/WAI/ARIA/apg/patterns/combobox/examples/combobox-select-only/")に従います。

## キーボード操作

| キー             | 説明                                                                                                       | 状態 |
| ---------------- | ---------------------------------------------------------------------------------------------------------- | ---- |
| `Tab`            | フォーカスがセレクトボックスに移動すると、最初の項目をフォーカスします。                                   | -    |
| `ArrowUp`        | 無効ではない前のオプションをフォーカスします。最初の項目の場合は無効ではない最後の項目をフォーカスします。 | -    |
| `ArrowDown`      | 無効ではない次のオプションをフォーカスします。最後の項目の場合は無効ではない最初の項目をフォーカスします。 | -    |
| `Space`, `Enter` | フォーカスしているオプションをアクティブにします。                                                         | -    |
| `Home`           | 無効ではない最初のオプションをフォーカスしてアクティブにします。                                           | -    |
| `End`            | 無効ではない最後のオプションをフォーカスしてアクティブにします。                                           |

## ARIAロールと属性

| コンポーネント           | ロールと属性         | 使い方                                                   |
| ------------------------ | -------------------- | -------------------------------------------------------- |
| `NativeSelect`           | `aria-hidden="true"` | 要素をアクセシビリティツリーから除外します。             |
| `select` <Tag>内部</Tag> | `aria-readonly`      | `isReadOnly`が設定されている場合は`"true"`を設定します。 |
