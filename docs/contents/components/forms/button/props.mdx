---
title: Button
description: >-
  `Button` is an interactive component that allows users to perform actions such
  as submitting forms and toggling modals.
package_name: "@yamada-ui/button"
with_description: true
tab: Props
---

## Props

### ButtonProps

<PropsCard
  id="button-colorscheme"
  name="colorScheme"
  type='"whiteAlpha" | "blackAlpha" | "gray" | "neutral" | "red" | "rose" | "pink" | "flashy" | "orange" | "amber" | "yellow" | "lime" | "green" | "emerald" | "teal" | "cyan" | "sky" | "blue" | "indigo" | "violet" | "purple" | "fuchsia"'
  description={"The visual color appearance of the component."}
  defaultValue="gray"
/>

<PropsCard
  id="button-disableripple"
  name="disableRipple"
  type="boolean"
  description={"If `true`, disable ripple effects when pressing a element."}
  defaultValue="false"
/>

<PropsCard
  id="button-isactive"
  name="isActive"
  type="boolean"
  description={"If `true`, the button is represented as active."}
  defaultValue="false"
/>

<PropsCard
  id="button-isdisabled"
  name="isDisabled"
  type="boolean"
  description={"If `true`, the button is disabled."}
  defaultValue="false"
/>

<PropsCard
  id="button-isloading"
  name="isLoading"
  type="boolean"
  description={"If `true`, the loading state of the button is represented."}
  defaultValue="false"
/>

<PropsCard
  id="button-lefticon"
  name="leftIcon"
  type="ReactElement<any, string | JSXElementConstructor<any>>"
  description={"The icon to display at the left side of the button."}
/>

<PropsCard
  id="button-loadingicon"
  name="loadingIcon"
  type='type ONLY_FOR_FORMAT =
  | "audio"
  | "grid"
  | ReactElement<any, string | JSXElementConstructor<any>>
  | "oval"
  | "dots"
  | "puff"
  | "rings"
  | "circles"'
  description={"The icon to display when the button is loading."}
/>

<PropsCard
  id="button-loadingplacement"
  name="loadingPlacement"
  type='"end" | "start"'
  description={
    "The placement of the loading indicator. Accepts `start` or `end`."
  }
  defaultValue="'start'"
/>

<PropsCard
  id="button-loadingtext"
  name="loadingText"
  type="string"
  description={"The text to display when the button is loading."}
/>

<PropsCard
  id="button-righticon"
  name="rightIcon"
  type="ReactElement<any, string | JSXElementConstructor<any>>"
  description={"The icon to display at the right side of the button."}
/>

<PropsCard
  id="button-size"
  name="size"
  type='"xs" | "sm" | "md" | "lg"'
  description={"The size of the Button."}
  defaultValue="md"
/>

<PropsCard
  id="button-variant"
  name="variant"
  type='"solid" | "outline" | "link" | "ghost" | "unstyled"'
  description={"The variant of the Button."}
  defaultValue="solid"
/>

### ButtonGroupProps

<PropsCard
  id="buttongroup-direction"
  name="direction"
  type='"-moz-initial" | "inherit" | "initial" | "revert" | "revert-layer" | "unset" | "column" | "column-reverse" | "row" | "row-reverse" | ResponsiveObject<FlexDirection> | ColorModeArray<...> | ResponsiveObject<...> | ColorModeArray<...> | ((theme: StyledTheme) => UIValue<...>)'
  description={"The CSS `flex-direction` property."}
/>

<PropsCard
  id="buttongroup-isattached"
  name="isAttached"
  type="boolean"
  description={
    "If `true`, the borderRadius of button that are direct children will be altered to look flushed together."
  }
  defaultValue="false"
/>

<PropsCard
  id="buttongroup-isdisabled"
  name="isDisabled"
  type="boolean"
  description={"If `true`, all wrapped button will be disabled."}
  defaultValue="false"
/>
