import { Bean } from "lucide-react"
import { component, Icon } from "../icon"

/**
 * `BeanIcon` is [Lucide](https://lucide.dev) SVG icon component.
 *
 * @see https://yamada-ui.com/components/icon
 */
export const BeanIcon = component(Icon)({ as: Bean })
