import { Users } from "lucide-react"
import { component, Icon } from "../icon"

/**
 * `UsersIcon` is [Lucide](https://lucide.dev) SVG icon component.
 *
 * @sees://yamada-ui.com/components/icon
 */
export const UsersIcon = component(Icon)({ as: Users })
