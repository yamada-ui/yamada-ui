---
title: Dialog
description: "`Dialog`は、ユーザーに実行の確認または中断に使用されるコンポーネントです。"
package_name: "@yamada-ui/dialog"
is_tabs: true
with_description: true
tab: 使い方
---

## インポート

```ts
import {
  Dialog,
  DialogOverlay,
  DialogCloseButton,
  DialogHeader,
  DialogBody,
  DialogFooter,
} from "@yamada-ui/react"
```

## 使い方

表示または非表示を制御するために、`isOpen`を設定します。

```tsx functional=true

```
