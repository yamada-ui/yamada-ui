---
title: FileButton
description: "`FileButton` is a button component used for users to select files."
package_name: "@yamada-ui/file-button"
with_description: true
tab: Props
---

## Props

### FileButton Props

<PropsCard name="errorBorderColor"
type='"link" | "border" | BorderColor | "current" | "whiteAlpha.50" | "whiteAlpha.100" | "whiteAlpha.200" | "whiteAlpha.300" | "whiteAlpha.400" | "whiteAlpha.500" | ... 253 more ... | ColorModeArray<...>'
description='The border color when the button is invalid.' />
<PropsCard name="isActive"
type='boolean'
description='If `true`, the button is represented as active.' />
<PropsCard name="isDisabled"
type='boolean'
description='If `true`, the button is disabled.

If `true`, the form control will be disabled.' />

<PropsCard
  name="isInvalid"
  type="boolean"
  description="If `true`, the form control will be invalid."
/>
<PropsCard
  name="isLoading"
  type="boolean"
  description="If `true`, the loading state of the button is represented."
/>
<PropsCard
  name="isReadOnly"
  type="boolean"
  description="If `true`, the form control will be readonly."
/>
<PropsCard
  name="isRequired"
  type="boolean"
  description="If `true`, the form control will be required."
/>
<PropsCard
  name="leftIcon"
  type="ReactElement<any, string | JSXElementConstructor<any>>"
  description="The icon to display at the left side of the button."
/>
<PropsCard
  name="loadingIcon"
  type='type ONLY_FOR_FORMAT =
  | "audio"
  | "grid"
  | ReactElement<any, string | JSXElementConstructor<any>>
  | "oval"
  | "dots"
  | "puff"
  | "rings"
  | "circles"'
  description="The icon to display when the button is loading."
/>
<PropsCard
  name="loadingPlacement"
  type='"end" | "start"'
  description="The placement of the loading indicator. Accepts `start` or `end`."
/>
<PropsCard
  name="loadingText"
  type="string"
  description="The text to display when the button is loading."
/>
<PropsCard
  name="onChange"
  type="(files: File[] | null) => void"
  description="Function to be called when a file change event occurs."
/>
<PropsCard
  name="resetRef"
  type="type ONLY_FOR_FORMAT =
  | ((instance: (() => void) | null) => void)
  | MutableRefObject<(() => void) | null>"
  description="Ref to a reset function."
/>
<PropsCard
  name="rightIcon"
  type="ReactElement<any, string | JSXElementConstructor<any>>"
  description="The icon to display at the right side of the button."
/>
