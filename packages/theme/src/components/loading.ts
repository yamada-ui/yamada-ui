import type { ComponentStyle } from "@yamada-ui/core"

export const Loading: ComponentStyle = {
  baseStyle: {},

  defaultProps: {
    variant: "oval",
    colorScheme: "primary",
    size: "1em",
  },
}
