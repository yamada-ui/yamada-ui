import { Tags } from "lucide-react"
import { component, Icon } from "../icon"

/**
 * `TagsIcon` is [Lucide](https://lucide.dev) SVG icon component.
 *
 * @sees://yamada-ui.com/components/icon
 */
export const TagsIcon = component(Icon)({ as: Tags })
