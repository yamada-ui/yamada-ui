---
title: Image
description: "`Image` is a component that displays images with fallback support."
package_name: "@yamada-ui/image"
order: 1
with_description: true
tab: Props
---

## Props

### ImageProps

<PropsCard
  id="image-crossorigin"
  name="crossOrigin"
  type='"" | "anonymous" | "use-credentials"'
  description={
    "The key used to set the crossOrigin on the HTMLImageElement into which the image will be loaded.\nThis tells the browser to request cross-origin access when trying to download the image data."
  }
/>

<PropsCard
  id="image-fallback"
  name="fallback"
  type="type ONLY_FOR_FORMAT =
  | string
  | ReactElement<any, string | JSXElementConstructor<any>>"
  description={
    "Fallback image `src` or element to show if image is loading or image fails."
  }
/>

<PropsCard
  id="image-fallbackstrategy"
  name="fallbackStrategy"
  type='"onError" | "beforeLoadOrError"'
  description={
    "- beforeLoadOrError: loads the fallbackImage while loading the src.\n- onError: loads the fallbackImage only if there is an error fetching the src."
  }
  defaultValue={"beforeLoadOrError"}
/>

<PropsCard
  id="image-fit"
  name="fit"
  type="UIValue<ObjectFit>"
  description={"The CSS `object-fit` property."}
/>

<PropsCard
  id="image-ignorefallback"
  name="ignoreFallback"
  type="boolean"
  description={
    "If `true`, opt out of the `fallbackSrc` logic and use as `img`."
  }
  defaultValue={"false"}
/>

<PropsCard
  id="image-loading"
  name="loading"
  type='"eager" | "lazy"'
  description={"The image `loading` attribute."}
/>

<PropsCard
  id="image-onerror"
  name="onError"
  type="ReactEventHandler<HTMLImageElement>"
  description={
    "A callback for when there was an error loading the image `src`."
  }
/>

<PropsCard
  id="image-onload"
  name="onLoad"
  type="ReactEventHandler<HTMLImageElement>"
  description={"A callback for when the image `src` has been loaded."}
/>

<PropsCard
  id="image-size"
  name="size"
  type='UIValue<number | "px" | "2xl" | "lg" | "md" | "sm" | "xl" | ({} & string) | "0.5" | "1" | "1.5" | "2" | "2.5" | "2xs" | "3" | "3.5" | "3xl" | "3xs" | "4" | "4.5" | "4xl" | "4xs" | "5" | "5.5" | ... 64 more ... | "min-intrinsic">'
  description={"The CSS `box-size` property."}
  deprecated={"Use `boxSize` instead."}
/>

<PropsCard
  id="image-sizes"
  name="sizes"
  type="string"
  description={"The image `sizes` attribute."}
/>

<PropsCard
  id="image-src"
  name="src"
  type="string"
  description={"The image `src` attribute."}
/>

<PropsCard
  id="image-srcset"
  name="srcSet"
  type="string"
  description={"The image `srcset` attribute."}
/>
