{
  "Link": {
    "external": {
      "type": "boolean",
      "defaultValue": false,
      "description": "If `true`, the link will open in new tab.",
      "required": false
    },
    "isExternal": {
      "type": "boolean",
      "defaultValue": false,
      "deprecated": "Use `external` instead",
      "description": "If `true`, the link will open in new tab.",
      "required": false
    }
  },
  "LinkBox": {},
  "LinkOverlay": {
    "external": {
      "type": "boolean",
      "defaultValue": false,
      "description": "If `true`, the link will open in new tab.",
      "required": false
    },
    "isExternal": {
      "type": "boolean",
      "defaultValue": false,
      "deprecated": "Use `external` instead",
      "description": "If `true`, the link will open in new tab.",
      "required": false
    }
  }
}
