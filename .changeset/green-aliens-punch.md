---
"@yamada-ui/react": minor
---

We exported the type alias `Item` for the component that has `items` in its props from each component's namespace. This now allows for type checking when declaring Props.
