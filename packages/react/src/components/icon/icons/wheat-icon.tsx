import { Wheat } from "lucide-react"
import { component, Icon } from "../icon"

/**
 * `WheatIcon` is [Lucide](https://lucide.dev) SVG icon component.
 *
 * @sees://yamada-ui.com/components/icon
 */
export const WheatIcon = component(Icon)({ as: Wheat })
