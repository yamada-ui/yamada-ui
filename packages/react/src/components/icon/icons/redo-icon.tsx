import { Redo } from "lucide-react"
import { component, Icon } from "../icon"

/**
 * `RedoIcon` is [Lucide](https://lucide.dev) SVG icon component.
 *
 * @sees://yamada-ui.com/components/icon
 */
export const RedoIcon = component(Icon)({ as: Redo })
