import { MailX } from "lucide-react"
import { component, Icon } from "../icon"

/**
 * `MailXIcon` is [Lucide](https://lucide.dev) SVG icon component.
 *
 * @sees://yamada-ui.com/components/icon
 */
export const MailXIcon = component(Icon)({ as: MailX })
