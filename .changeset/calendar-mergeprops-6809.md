---
"@yamada-ui/react": patch
---

Merge `getRootProps` and `getDayProps` in the calendar hooks with `mergeProps` so user `className`, `style`, `ref`, and event handlers combine correctly with hook defaults.
