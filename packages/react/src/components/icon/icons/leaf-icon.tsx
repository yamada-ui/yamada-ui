import { Leaf } from "lucide-react"
import { component, Icon } from "../icon"

/**
 * `LeafIcon` is [Lucide](https://lucide.dev) SVG icon component.
 *
 * @see https://yamada-ui.com/components/icon
 */
export const LeafIcon = component(Icon)({ as: Leaf })
