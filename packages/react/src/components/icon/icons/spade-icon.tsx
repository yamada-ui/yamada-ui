import { Spade } from "lucide-react"
import { component, Icon } from "../icon"

/**
 * `SpadeIcon` is [Lucide](https://lucide.dev) SVG icon component.
 *
 * @sees://yamada-ui.com/components/icon
 */
export const SpadeIcon = component(Icon)({ as: Spade })
