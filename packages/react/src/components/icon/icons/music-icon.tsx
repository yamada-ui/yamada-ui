import { Music } from "lucide-react"
import { component, Icon } from "../icon"

/**
 * `MusicIcon` is [Lucide](https://lucide.dev) SVG icon component.
 *
 * @sees://yamada-ui.com/components/icon
 */
export const MusicIcon = component(Icon)({ as: Music })
