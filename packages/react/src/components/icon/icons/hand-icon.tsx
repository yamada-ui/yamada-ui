import { Hand } from "lucide-react"
import { component, Icon } from "../icon"

/**
 * `HandIcon` is [Lucide](https://lucide.dev) SVG icon component.
 *
 * @sees://yamada-ui.com/components/icon
 */
export const HandIcon = component(Icon)({ as: Hand })
