---
title: FocusLock
description: >-
  `FocusLock`は、モーダルやダイアログなどの要素でフォーカスを制限し、フォーカスをその範囲内にロックすることで、アクセシビリティを向上させるコンポーネントです。
package_name: "@yamada-ui/focus-lock"
order: 1
with_description: true
tab: Props
---

## Props

### FocusLockProps

<PropsCard
  id="focuslock-autofocus"
  name="autoFocus"
  type="boolean"
  description={
    "If `true`, the first focusable element within the `children` will auto-focused once `FocusLock` mounts."
  }
  defaultValue={"false"}
/>

<PropsCard
  id="focuslock-contentref"
  name="contentRef"
  type="RefObject<HTMLElement>"
  description={"The `ref` of the wrapper for which the focus-lock wraps."}
/>

<PropsCard
  id="focuslock-disabled"
  name="disabled"
  type="boolean"
  description={"If `true`, focus trapping will be disabled."}
  defaultValue={"false"}
/>

<PropsCard
  id="focuslock-finalfocusref"
  name="finalFocusRef"
  type="RefObject<FocusableElement>"
  description={
    "`ref` of the element to return focus to when `FocusLock` unmounts."
  }
/>

<PropsCard
  id="focuslock-initialfocusref"
  name="initialFocusRef"
  type="RefObject<FocusableElement>"
  description={"`ref` of the element to receive focus initially."}
/>

<PropsCard
  id="focuslock-isdisabled"
  name="isDisabled"
  type="boolean"
  description={"If `true`, focus trapping will be disabled."}
  deprecated={"Use `disabled` instead."}
  defaultValue={"false"}
/>

<PropsCard
  id="focuslock-lockfocusacrossframes"
  name="lockFocusAcrossFrames"
  type="boolean"
  description={
    "Enables aggressive focus capturing within iframes.\n- If `true`: keep focus in the lock, no matter where lock is active.\n- If `false`:  allows focus to move outside of iframe."
  }
  defaultValue={"false"}
/>

<PropsCard
  id="focuslock-persistentfocus"
  name="persistentFocus"
  type="boolean"
  description={
    "If `true`, disables text selections inside, and outside focus lock."
  }
  defaultValue={"false"}
/>

<PropsCard
  id="focuslock-restorefocus"
  name="restoreFocus"
  type="boolean"
  description={
    "If `true`, focus will be restored to the element that triggered the `FocusLock` once it unmounts."
  }
  defaultValue={"false"}
/>
