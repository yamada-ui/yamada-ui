"use client"

import { View } from "lucide-react"
import { component, Icon } from "../icon"

/**
 * `ViewIcon` is [Lucide](https://lucide.dev) SVG icon component.
 *
 * @see https://yamada-ui.com/components/icon
 */
export const ViewIcon = component(Icon)({ as: View })
