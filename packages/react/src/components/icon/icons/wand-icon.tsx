import { Wand } from "lucide-react"
import { component, Icon } from "../icon"

/**
 * `WandIcon` is [Lucide](https://lucide.dev) SVG icon component.
 *
 * @sees://yamada-ui.com/components/icon
 */
export const WandIcon = component(Icon)({ as: Wand })
