import { Slice } from "lucide-react"
import { component, Icon } from "../icon"

/**
 * `SliceIcon` is [Lucide](https://lucide.dev) SVG icon component.
 *
 * @sees://yamada-ui.com/components/icon
 */
export const SliceIcon = component(Icon)({ as: Slice })
