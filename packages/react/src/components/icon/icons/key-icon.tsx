"use client"

import { Key } from "lucide-react"
import { component, Icon } from "../icon"

/**
 * `KeyIcon` is [Lucide](https://lucide.dev) SVG icon component.
 *
 * @see https://yamada-ui.com/docs/components/icon
 */
export const KeyIcon = component(Icon)({ as: Key })
