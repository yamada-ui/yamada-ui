"use client"

import { Bed } from "lucide-react"
import { component, Icon } from "../icon"

/**
 * `BedIcon` is [Lucide](https://lucide.dev) SVG icon component.
 *
 * @see https://yamada-ui.com/docs/components/icon
 */
export const BedIcon = component(Icon)({ as: Bed })
