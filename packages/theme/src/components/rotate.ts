import type { ComponentStyle } from "@yamada-ui/core"

export const Rotate: ComponentStyle<"Rotate"> = {
  baseStyle: {
    userSelect: "none",
    cursor: "pointer",
  },

  variants: {},

  sizes: {},

  defaultProps: {},
}
