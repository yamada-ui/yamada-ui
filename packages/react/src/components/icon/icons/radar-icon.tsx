import { Radar } from "lucide-react"
import { component, Icon } from "../icon"

/**
 * `RadarIcon` is [Lucide](https://lucide.dev) SVG icon component.
 *
 * @sees://yamada-ui.com/components/icon
 */
export const RadarIcon = component(Icon)({ as: Radar })
