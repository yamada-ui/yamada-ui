---
title: ColorSwatch
description: "`ColorPicker`は、色の見本を表示するコンポーネントです。"
package_name: "@yamada-ui/color-picker"
with_description: true
tab: テーマ
---

## テーマ

`ColorSwatch`は、[複数パーツのコンポーネント](/styled-system/theming/component-styles#複数パーツのコンポーネント)です。

:::note
コンポーネントのスタイルを変更したい場合は、[こちら](/styled-system/theming/customize-theme#コンポーネントのスタイルを変更する)をご覧ください。
:::

```ts
export const ColorSwatch: ComponentMultiStyle = {
  baseStyle: {
    container: {},
    overlay: { boxSize: "full" },
  },

  variants: {
    basic: {
      container: { rounded: "md" },
      overlay: { rounded: "md" },
    },
    rounded: {
      container: { rounded: "full" },
      overlay: { rounded: "full" },
    },
  },

  sizes: {
    sm: {
      container: { boxSize: "6" },
    },
    md: {
      container: { boxSize: "8" },
    },
    lg: {
      container: { boxSize: "10" },
    },
  },

  defaultProps: {
    variant: "basic",
    size: "md",
  },
}
```
