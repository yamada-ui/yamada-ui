import { Move } from "lucide-react"
import { component, Icon } from "../icon"

/**
 * `MoveIcon` is [Lucide](https://lucide.dev) SVG icon component.
 *
 * @sees://yamada-ui.com/components/icon
 */
export const MoveIcon = component(Icon)({ as: Move })
