---
title: Installation
description: How to install and use Yamada UI in the project.
order: 1
is_tabs: true
tab: All
---

:::note
If you are creating a website or landing page, rather than an app, we recommend [the minimal setup](/getting-started/installation/minimal).
:::

## Install the Packages

With Yamada UI, by just installing `@yamada-ui/react`, you can use most of the components and hooks.

Run one of the following commands in the terminal.

<PackageManagers packageNameOrCommand='@yamada-ui/react' />

If you're using tables, calendars, carousels, etc., you'll need to install them separately.

<SelectPackageManagers
  packages={[
    {
      name: '@yamada-ui/table',
      description: 'Provides a handy table component using `@tanstack/react-table`.',
      isDefaultCheck: true,
    },
    {
      name: '@yamada-ui/calendar',
      description: 'Provides a handy calendar and date picker component using `dayjs`.',
    },
    {
      name: '@yamada-ui/carousel',
      description: 'Provides a convenient carousel component using `embla-carousel-react`.',
    },
    {
      name: '@yamada-ui/dropzone',
      description: 'Provides a convenient dropzone component using `react-dropzone`.',
    },
    {
      name: '@yamada-ui/markdown',
      description:
        'Provides a handy markdown component using `react-markdown` and `react-syntax-highlighter`.',
    },
    {
      name: '@yamada-ui/fontawesome',
      description: 'Provides a component for conveniently using `Font Awesome`.',
    },
  ]}
/>

:::note status=warning
Yamada UI is only compatible with React 18. If you're using React 17 or earlier, please upgrade to React 18.
:::

## Add the `UIProvider`

After installing Yamada UI, add `UIProvider` to the root of the application.

```tsx live=false highlight={5,7}
import { UIProvider } from '@yamada-ui/react'

const App = () => {
  return (
    <UIProvider>
      <YourApplication />
    </UIProvider>
  )
}
```

## Using Components

Once you've added the `UIProvider`, call the components within the application.

```tsx live=false
import { Button } from '@yamada-ui/react'

const Demo = () => {
  return <Button>Click me!</Button>
}
```

That's it! You've successfully set up Yamada UI!

## Start Learning

Why not learn the basics and themes to get to know Yamada UI better? 😎

<CardContainer>
  <LinkCard href='/getting-started/basics' />
  <LinkCard
    href='/styled-system/theming'
    title='Learn about Themes'
    description="Get to know Yamada UI's default theme and learn how to create and modify the values for colors, fonts, and other theme attributes."
  />
  <LinkCard
    href='/components'
    title='Explore Components'
    description='Yamada UI offers over 100 flexible components. All components support animations and dark mode.'
  />
  <LinkCard
    href='https://github.com/hirotomoyamada/yamada-ui'
    title='Explore the Source Code'
    description='The Yamada UI package and documentation site are open source. If you like Yamada UI, please give it a star.'
  />
</CardContainer>
