---
title: AreaChart
description: "`AreaChart`は、複数のデータを比較するためのエリアチャートを描画するコンポーネントです。"
package_name: "@yamada-ui/charts"
label: New
with_description: true
tab: テーマ
---

## テーマ

`AreaChart`は、[単一パーツのコンポーネント](/styled-system/theming/component-styles#単一パーツのコンポーネント)です。

:::note
`AreaChart`は、[LineChart](/components/feedback/line-chart)のスタイルを継承しています。
:::

:::note
コンポーネントのスタイルを変更したい場合は、[こちら](/styled-system/theming/customize-theme#コンポーネントのスタイルを変更する)をご覧ください。
:::

```ts
export const AreaChart: ComponentMultiStyle = mergeMultiStyle(LineChart, {
  baseStyle: {
    area: {},
  },
})({ omit: ["line"] })
```
