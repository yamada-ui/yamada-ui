import { Cake } from "lucide-react"
import { component, Icon } from "../icon"

/**
 * `CakeIcon` is [Lucide](https://lucide.dev) SVG icon component.
 *
 * @see https://yamada-ui.com/components/icon
 */
export const CakeIcon = component(Icon)({ as: Cake })
