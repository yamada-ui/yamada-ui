---
title: useBreakpointEffect
description: >-
  `useBreakpointEffect`は、初回のレンダリング時にはエフェクトをスキップし、ブレイクポイントが変更されたときにエフェクトを実行するカスタムフックです。
package_name: "@yamada-ui/use-breakpoint"
with_description: true
---

## インポート

```ts
import { useUpdateBreakpointEffect } from "@yamada-ui/react"
```

## 使い方

エフェクトは、テーマの[ブレイクポイント](/styled-system/theming/default-theme#breakpoints)を提供します。

```tsx functional=true
const [device, setDevice] = useBreakpointState({
  base: "desktop",
  md: "tablet",
  sm: "mobile",
})

useUpdateBreakpointEffect((breakpoint) => {
  if (breakpoint === "sm") {
    setDevice("mobile")
  } else if (breakpoint === "md") {
    setDevice("tablet")
  } else {
    setDevice("desktop")
  }
}, [])

return (
  <Box
    bg={{
      base: "red.500",
      xl: "blue.500",
      lg: "green.500",
      md: "yellow.500",
      sm: "purple.500",
    }}
    p="md"
    rounded="md"
    color="white"
    transitionProperty="all"
    transitionDuration="slower"
  >
    The current device is "{device}"
  </Box>
)
```

:::note
レスポンシブスタイルをもっと学びたい場合は、[こちら](/styled-system/responsive-styles)をご覧ください。
:::
