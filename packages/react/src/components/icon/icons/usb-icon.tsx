import { Usb } from "lucide-react"
import { component, Icon } from "../icon"

/**
 * `UsbIcon` is [Lucide](https://lucide.dev) SVG icon component.
 *
 * @see https://yamada-ui.com/components/icon
 */
export const UsbIcon = component(Icon)({ as: Usb })
