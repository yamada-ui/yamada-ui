---
title: Get Started
description: How to install and use Yamada UI in your project.
---

## Pick your framework

::::card-group
:::card{href="/docs/get-started/frameworks/next-app"}

### Next.js (App)

A guide for installing and using Yamada UI with Next.js app directory.

:::
:::card{href="/docs/get-started/frameworks/next-pages"}

### Next.js (Pages)

A guide for installing and using Yamada UI with Next.js pages directory.

:::
:::card{href="/docs/get-started/frameworks/remix"}

### Remix

A guide for installing and using Yamada UI with Remix projects.

:::
:::card{href="/docs/get-started/frameworks/vite"}

### Vite

A guide for installing and using Yamada UI with Vite.js projects.

:::
::::

## Installation

To install Yamada UI in your project, please follow the steps below.

::::steps

### Install the package

Yamada UI can be installed with `@yamada-ui/react` only, and all components and hooks can be used.

:::code-group

```bash [pnpm]
pnpm add @yamada-ui/react
```

```bash [npm]
npm install @yamada-ui/react
```

```bash [yarn]
yarn add @yamada-ui/react
```

```bash [bun]
bun add @yamada-ui/react
```

:::

### Initialize the project

When the command is executed, files and folders necessary for your project will be created.

:::code-group

```bash [pnpm]
pnpm dlx @yamada-ui/cli init
```

```bash [npm]
npx @yamada-ui/cli init
```

```bash [yarn]
yarn dlx @yamada-ui/cli init
```

```bash [bun]
bunx @yamada-ui/cli init
```

:::

### Add UIProvider

After installing Yamada UI, add UIProvider to the root of your application.

```tsx
import { UIProvider } from "@yamada-ui/react"

const App = () => {
  return (
    <UIProvider>
      <YourApplication />
    </UIProvider>
  )
}
```

### Use components

Once you've added `UIProvider`, use the components in your application.

```tsx
import { Button } from "@yamada-ui/react"

const App = () => {
  return <Button>Click me</Button>
}
```

With this, the setup for Yamada UI is complete!

::::
