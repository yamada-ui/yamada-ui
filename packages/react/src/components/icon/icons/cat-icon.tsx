import { Cat } from "lucide-react"
import { component, Icon } from "../icon"

/**
 * `CatIcon` is [Lucide](https://lucide.dev) SVG icon component.
 *
 * @see https://yamada-ui.com/components/icon
 */
export const CatIcon = component(Icon)({ as: Cat })
