"use client"

import { Cpu } from "lucide-react"
import { component, Icon } from "../icon"

/**
 * `CpuIcon` is [Lucide](https://lucide.dev) SVG icon component.
 *
 * @see https://yamada-ui.com/docs/components/icon
 */
export const CpuIcon = component(Icon)({ as: Cpu })
