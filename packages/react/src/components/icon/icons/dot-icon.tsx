"use client"

import { Dot } from "lucide-react"
import { component, Icon } from "../icon"

/**
 * `DotIcon` is [Lucide](https://lucide.dev) SVG icon component.
 *
 * @see https://yamada-ui.com/docs/components/icon
 */
export const DotIcon = component(Icon)({ as: Dot })
