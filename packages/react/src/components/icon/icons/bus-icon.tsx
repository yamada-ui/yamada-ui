import { Bus } from "lucide-react"
import { component, Icon } from "../icon"

/**
 * `BusIcon` is [Lucide](https://lucide.dev) SVG icon component.
 *
 * @see Docs https://yamada-ui.com/components/icon
 */
export const BusIcon = component(Icon)({ as: Bus })
