"use client"

import { Sofa } from "lucide-react"
import { component, Icon } from "../icon"

/**
 * `SofaIcon` is [Lucide](https://lucide.dev) SVG icon component.
 *
 * @see https://yamada-ui.com/components/icon
 */
export const SofaIcon = component(Icon)({ as: Sofa })
