import {
  ui,
  forwardRef,
  useMultiComponentStyle,
  omitThemeProps,
  CSSUIObject,
  HTMLUIProps,
  ThemeProps,
} from '@yamada-ui/system'
import { createContext, cx } from '@yamada-ui/utils'

type {{capitalize componentName}}Options = {}

export type {{capitalize componentName}}Props = HTMLUIProps<'div'> & ThemeProps<'{{capitalize componentName}}'> & {{capitalize componentName}}Options

type {{capitalize componentName}}Context = Record<string, CSSUIObject>

const [{{capitalize componentName}}Provider, use{{capitalize componentName}}] = createContext<{{capitalize componentName}}Context>({
  strict: false,
  name: '{{capitalize componentName}}Context',
})

export { use{{capitalize componentName}} }

export const {{capitalize componentName}} = forwardRef<{{capitalize componentName}}Props, 'div'>((props, ref) => {
  const styles = useMultiComponentStyle('{{capitalize componentName}}', props)
  const { className, ...rest } = omitThemeProps(props)

  const css: CSSUIObject = {}

  return (
    <{{capitalize componentName}}Provider value={styles}>
      <ui.div ref={ref} className={cx('ui-{{dashCase componentName}}', className)} __css={css} {...rest} />
    </{{capitalize componentName}}Provider>
  )
})
