---
title: CircleProgress
description: "`CircleProgress` is a component that displays progress in a circular progress bar."
package_name: "@yamada-ui/react"
is_tabs: true
with_description: true
tab: Usage
---

## Import

```ts
import { CircleProgress, CircleProgressLabel } from "@yamada-ui/react"
```

## Usage

```tsx
<CircleProgress value={19} />
```

### Change Size

You can change the `boxSize` (`width`, `height`) by setting a string or number to the `size`.

```tsx
<CircleProgress value={18} size={32} />
```

### Change Thickness

If you want to change the thickness of the progress bar, set a string or number to `thickness`.

```tsx
<CircleProgress value={31} thickness={1} />
```

### Add Label

```tsx
<CircleProgress
  size="120px"
  min={0}
  max={1000}
  value={444}
  color="purple.500"
  trackColor="green.300"
>
  <CircleProgressLabel color="purple.500">444%</CircleProgressLabel>
</CircleProgress>
```

### Make Corners Rounded

You can make the progress bar rounded by setting `isRounded` to `true`.

```tsx
<CircleProgress value={18} isRounded />
```

### Apply Animation

Setting `isAnimation` to `true` will apply animation.

```tsx
<CircleProgress value={18} isAnimation />
```
