---
title: Accordion
description: "`Accordion`は、情報を展開または折りたたんで表示するリストのコンポーネントです。"
package_name: "@yamada-ui/accordion"
with_description: true
tab: Props
order: 1
---

## Props

### AccordionProps

<PropsCard
  id="accordion-colorscheme"
  name="colorScheme"
  type='"blackAlpha" | "whiteAlpha" | "amber" | "blue" | "info" | "link" | "primary" | "cyan" | "emerald" | "flashy" | "fuchsia" | "gray" | "green" | "success" | "indigo" | "lime" | "neutral" | "orange" | "warning" | "pink" | "purple" | "red" | "danger" | "rose" | "sky" | "teal" | "violet" | "secondary" | "yellow"'
  description={"The visual color appearance of the component."}
/>

<PropsCard
  id="accordion-defaultindex"
  name="defaultIndex"
  type="number | number[]"
  description={"The initial index(es) of the accordion item to expand."}
/>

<PropsCard
  id="accordion-icon"
  name="icon"
  type="string | number | boolean | ReactElement<any, string | JSXElementConstructor<any>> | Iterable<ReactNode> | ReactPortal | ((props: { ...; }) => ReactNode)"
  description={"The accordion icon for all items to use."}
/>

<PropsCard
  id="accordion-iconhidden"
  name="iconHidden"
  type="boolean"
  description={"If `true`, hide the accordion icon for all items."}
  defaultValue={"false"}
/>

<PropsCard
  id="accordion-index"
  name="index"
  type="number | number[]"
  description={"The index(es) of the accordion item to expand."}
/>

<PropsCard
  id="accordion-ismultiple"
  name="isMultiple"
  type="boolean"
  description={"If `true`, multiple accordion items can be expanded at once."}
  deprecated={"Use `multiple` instead."}
  defaultValue={"false"}
/>

<PropsCard
  id="accordion-istoggle"
  name="isToggle"
  type="boolean"
  description={"If `true`, any expanded accordion item can be collapsed again."}
  deprecated={"Use `toggle` instead."}
  defaultValue={"false"}
/>

<PropsCard
  id="accordion-multiple"
  name="multiple"
  type="boolean"
  description={"If `true`, multiple accordion items can be expanded at once."}
  defaultValue={"false"}
/>

<PropsCard
  id="accordion-onchange"
  name="onChange"
  type="(index: number | number[]) => void"
  description={
    "The callback invoked when accordion items are expanded or collapsed."
  }
/>

<PropsCard
  id="accordion-size"
  name="size"
  type="string"
  description={"The size of the Accordion."}
/>

<PropsCard
  id="accordion-toggle"
  name="toggle"
  type="boolean"
  description={"If `true`, any expanded accordion item can be collapsed again."}
  defaultValue={"false"}
/>

<PropsCard
  id="accordion-variant"
  name="variant"
  type='"basic" | "card" | "unstyled"'
  description={"The variant of the Accordion."}
  defaultValue={'"basic"'}
/>

### AccordionItemProps

<PropsCard
  id="accordionitem-disabled"
  name="disabled"
  type="boolean"
  description={"If `true`, the accordion item will be disabled."}
  defaultValue={"false"}
/>

<PropsCard
  id="accordionitem-icon"
  name="icon"
  type="string | number | boolean | ReactElement<any, string | JSXElementConstructor<any>> | Iterable<ReactNode> | ReactPortal | ((props: { ...; }) => ReactNode)"
  description={"The accordion icon to use."}
/>

<PropsCard
  id="accordionitem-isdisabled"
  name="isDisabled"
  type="boolean"
  description={"If `true`, the accordion item will be disabled."}
  deprecated={"Use `disabled` instead."}
  defaultValue={"false"}
/>

<PropsCard
  id="accordionitem-label"
  name="label"
  type="string | number | boolean | ReactElement<any, string | JSXElementConstructor<any>> | Iterable<ReactNode> | ReactPortal | ((props: { ...; }) => ReactNode)"
  description={"The accordion label to use."}
/>

### AccordionLabelProps

<PropsCard
  id="accordionlabel-containerprops"
  name="containerProps"
  type="HTMLUIProps"
  description={"Props the container element."}
/>

<PropsCard
  id="accordionlabel-icon"
  name="icon"
  type="string | number | boolean | ((props: { isDisabled: boolean; isExpanded: boolean; disabled?: boolean | undefined; expanded?: boolean | undefined; }) => ReactNode) | ReactElement<any, string | JSXElementConstructor<...>> | Iterable<...> | ReactPortal"
  description={"The accordion icon to use."}
/>

### AccordionPanelProps

<PropsCard
  id="accordionpanel-animationopacity"
  name="animationOpacity"
  type="boolean"
  description={"If `true`, the opacity of the content will be animated."}
  defaultValue={"true"}
/>

<PropsCard
  id="accordionpanel-delay"
  name="delay"
  type="number | MotionLifecycleProps<number>"
  description={"Custom `delay` definition for `enter` and `exit`."}
/>

<PropsCard
  id="accordionpanel-duration"
  name="duration"
  type="number | MotionLifecycleProps<number>"
  description={"Custom `duration` definition for `enter` and `exit`."}
/>

<PropsCard
  id="accordionpanel-endingheight"
  name="endingHeight"
  type="string | number"
  description={"The height you want the content in its expanded state."}
  defaultValue={"auto"}
/>

<PropsCard
  id="accordionpanel-enter"
  name="enter"
  type="any"
  description={"Custom `enter`."}
/>

<PropsCard
  id="accordionpanel-exit"
  name="exit"
  type="any"
  description={"Custom `exit`."}
/>

<PropsCard
  id="accordionpanel-initial"
  name="initial"
  type="any"
  description={"Custom `initial`."}
/>

<PropsCard
  id="accordionpanel-open"
  name="open"
  type="boolean"
  description={"Show the component. triggers when enter or exit states."}
/>

<PropsCard
  id="accordionpanel-startingheight"
  name="startingHeight"
  type="string | number"
  description={"The height you want the content in its collapsed state."}
  defaultValue={"0"}
/>

<PropsCard
  id="accordionpanel-transition"
  name="transition"
  type="MotionLifecycleProps<Transition>"
  description={"Custom `transition` definition for `enter` and `exit`."}
/>

<PropsCard
  id="accordionpanel-transitionend"
  name="transitionEnd"
  type="MotionLifecycleProps<Target>"
  description={"Custom `transitionEnd` definition for `enter` and `exit`."}
/>

<PropsCard
  id="accordionpanel-unmountonexit"
  name="unmountOnExit"
  type="boolean"
  description={
    "If `true`, the element will unmount when `isOpen={false}` and animation is done."
  }
/>
