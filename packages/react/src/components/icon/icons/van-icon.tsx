"use client"

import { Van } from "lucide-react"
import { component, Icon } from "../icon"

/**
 * `VanIcon` is [Lucide](https://lucide.dev) SVG icon component.
 *
 * @see https://yamada-ui.com/docs/components/icon
 */
export const VanIcon = component(Icon)({ as: Van })
